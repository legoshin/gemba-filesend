---
phase: 03-download-page-redesign-dark-mode-complete
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02, 03-03]
files_modified: []
autonomous: false
requirements: [PAGE-03, DARK-02]
must_haves:
  truths:
    - "Every page (home, upload, download) and every shared component renders correctly under light, dark, AND system"
    - "The DARK-02 trouble-spot checklist passes: toasts, native <select>, focus rings, app-shell + mobile tab bar, scrollbars, Public Sans in production, all download-page states, and the 3-way menu"
    - "'System' follows an OS theme switch live without a manual re-select, and the 3-way menu correctly marks 'System' as selected"
    - "A human has signed off visual correctness on the deployed Vercel preview"
  artifacts:
    - path: ".planning/phases/03-download-page-redesign-dark-mode-complete/03-04-SUMMARY.md"
      provides: "Recorded DARK-02 sign-off with the checklist results and the reviewed preview commit"
      contains: "DARK-02"
  key_links:
    - from: "deployed Vercel preview"
      to: "DARK-02 checklist"
      via: "human visual sweep across light/dark/system on every page"
      pattern: "sign-off"
---

<objective>
Close DARK-02: with the download page reskinned (Plan 01), the 3-way theme control
shipped (Plan 02), and the Public Sans production drop fixed (Plan 03), run the explicit
D-04 trouble-spot checklist across light / dark / system on every page and require a
blocking human visual sign-off (same gate style as Phase 2's 02-03). A bare visual skim
is not sufficient — every row of the UI-SPEC DARK-02 completeness bar is checked.

Purpose: Success criterion 3 asserts NO unstyled or mis-themed element remains anywhere
in the app, in all three theme modes. This is the phase's verification backstop.
Output: A recorded sign-off (SUMMARY) capturing the checklist results and the reviewed
preview commit; DARK-02 and PAGE-03 marked verifiable.
</objective>

## Phase Goal (user story)

**As a** user of the whole app, **I want to** switch between Light, Dark, and System on
any page and never see an unstyled, mis-colored, or illegible element, **so that** the
redesign is trustworthy end-to-end across web, PWA, and Android TWA.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-download-page-redesign-dark-mode-complete/03-CONTEXT.md
@.planning/phases/03-download-page-redesign-dark-mode-complete/03-UI-SPEC.md
@.planning/phases/02-upload-page-redesign/02-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + source audit sweep (evidence for the sign-off)</name>
  <read_first>
    - .planning/phases/03-download-page-redesign-dark-mode-complete/03-UI-SPEC.md (DARK-02 completeness bar — the authoritative checklist)
    - src/app/download/page.tsx (as left by Plan 01)
    - src/components/theme-toggle.tsx and src/components/ui/dropdown-menu.tsx (as left by Plan 02)
    - src/app/layout.tsx (as left by Plan 03)
  </read_first>
  <action>
    Produce the automated evidence the human gate will rely on. Run `npx tsc --noEmit`,
    `npm run lint`, and `npm run build` and confirm all exit 0. Then run a source-audit
    grep sweep across the redesigned surfaces to confirm no legacy styling leaked through:
    no `lucide-react` import in `src/app/download/page.tsx`; no `bg-popover`/`shadow-md`
    in the reskinned `DropdownMenuContent`; no `resolvedTheme` in `theme-toggle.tsx`; and
    the Public Sans request present in `src/app/layout.tsx`. Record the exact preview
    deploy commit SHA (the current `feat/android-twa-pwa` HEAD after Plans 01–03 merge) so
    the human reviews a known build. Do not modify any source files in this task — it is a
    read-only audit that assembles the checklist inputs.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && ! grep -q "lucide-react" src/app/download/page.tsx && ! grep -Eq "bg-popover|shadow-md" src/components/ui/dropdown-menu.tsx && ! grep -q "resolvedTheme" src/components/theme-toggle.tsx && grep -Eq "Public\\+Sans|next/font" src/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit`, `npm run lint`, and `npm run build` all exit 0.
    - Source sweep confirms: zero `lucide-react` in `download/page.tsx`; no `bg-popover`/`shadow-md` in the reskinned `DropdownMenuContent`; no `resolvedTheme` in `theme-toggle.tsx`; Public Sans request present in `layout.tsx`.
    - The reviewed preview commit SHA is recorded for the sign-off.
    - No source files are modified by this task.
  </acceptance_criteria>
  <done>
    Build/typecheck/lint are green, the source audit confirms no legacy styling remains on
    the redesigned surfaces, and the exact preview commit is pinned for human review.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: DARK-02 completeness — blocking human visual sign-off (light / dark / system)</name>
  <read_first>
    - .planning/phases/03-download-page-redesign-dark-mode-complete/03-UI-SPEC.md (DARK-02 completeness bar — verify every row)
    - .planning/phases/03-download-page-redesign-dark-mode-complete/03-CONTEXT.md (D-04 — explicit trouble-spot checklist + blocking sign-off)
    - .planning/phases/02-upload-page-redesign/02-03-PLAN.md (the precedent gate format)
  </read_first>
  <what-built>
    Plans 01–03 delivered: the fully reskinned download page (all states + D-01 handled
    error cards + inline password error + D-03 secure row), the 3-way light/dark/system
    theme control (D-02) with a Gemba-reskinned dropdown popover, and the Public Sans
    production-font fix. Task 1 confirmed build/typecheck/lint green and audited the source.
  </what-built>
  <how-to-verify>
    On the deployed Vercel preview (not `next dev`) at the recorded commit, walk the full
    UI-SPEC DARK-02 completeness bar. For EACH surface below, check LIGHT, DARK, and SYSTEM
    (three modes each) — no unstyled element, no wrong color resolution, no illegible
    contrast:
    1. Home page (Phase 1) — spot-check all three modes.
    2. Upload page (Phase 2) — spot-check all three modes; include the native `<select>`
       expiry unit.
    3. Download page — input state; preview state (Chips, the D-03 secure row, password
       field); downloading state (the new `Loading03` spinner — confirm the glyph reads
       correctly when spun, else note the `Loading01` fallback); done state; and all three
       D-01 error cards (invalid link / file not found / link expired) plus the inline
       wrong-password error.
    4. 3-way theme menu (D-02) — open it in each mode; confirm the popover uses Gemba
       surface/shadow (not raw shadcn), the correct item is checked, and picking each of
       Light/Dark/System applies and persists across a reload.
    5. "System" behavior — with "System" selected, switch the OS theme while the app is
       open and confirm `resolvedTheme` follows live without a manual re-select, and the
       menu still shows "System" checked (not silently "Light"/"Dark").
    6. Shared shell — `app-shell.tsx` top bar/sidebar and `mobile-tab-bar.tsx` at mobile
       width, all three modes.
    7. `sonner` toasts, focus rings (must stay ink-based, not accent-blue), and scrollbars
       against dark surfaces.
    8. Public Sans renders on the deployed build (typeface is Public Sans, not a system
       fallback) in all three modes.
    Reply "approved" if every row passes, or list the specific failing surface + mode so a
    --gaps plan can target it.
  </how-to-verify>
  <acceptance_criteria>
    - Every row of the UI-SPEC DARK-02 completeness bar has been checked in light, dark, AND system on the deployed preview.
    - The 3-way menu shows the correct selected state (including "System") and persists selection across reload.
    - "System" follows a live OS theme switch without manual re-select.
    - Public Sans renders in the production build across all three modes.
    - A human sign-off ("approved") is recorded in the SUMMARY, along with the reviewed commit SHA; any listed failures are captured as gaps.
  </acceptance_criteria>
  <resume-signal>Type "approved" to close DARK-02, or list the failing surface(s) + theme mode(s) to spin up a --gaps plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendered UI to human reviewer | The sign-off must confirm the encryption key never surfaces in any rendered state (no key in error copy, DOM attributes, or the secure-reassurance row) — a visual confirmation of the Plan 01 key-leakage mitigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | rendered download-page states | mitigate | The human sign-off explicitly confirms no decryption key / URL-fragment value is visible in any state card, chip, secure row, or error message across all three themes. |
| T-03-09 | Repudiation | sign-off record | mitigate | The SUMMARY records who approved, the reviewed commit SHA, and the checklist outcome, so the DARK-02 close is auditable. |
| T-03-SC | Tampering (supply chain) | npm installs | accept | Verification-only plan; no installs, no source changes, no `components.json` change. |
</threat_model>

## Artifacts this phase produces (this plan)

- `.planning/phases/03-download-page-redesign-dark-mode-complete/03-04-SUMMARY.md` — the
  recorded DARK-02 sign-off (checklist results, reviewed commit SHA, approver).
- No source files modified; no new symbols; no new npm dependencies.

<verification>
- Task 1 automated gate: `npx tsc --noEmit`, `npm run lint`, `npm run build` all exit 0;
  source sweep clean; preview commit pinned.
- Task 2 blocking human gate: every DARK-02 completeness-bar row confirmed in
  light/dark/system on the deployed preview; "approved" recorded. This gate is
  never auto-approvable — it requires an explicit human signal.
</verification>

<success_criteria>
- DARK-02: every page and every shared component renders correctly in light, dark, and
  system via `next-themes` — no unstyled or mis-themed element remains (success criterion 3).
- PAGE-03: the redesigned download page is visually confirmed correct across all states
  and themes (success criteria 1–2).
- Success criterion 4 (download/decrypt unchanged) is re-confirmed during the download-flow
  walk of the sign-off.
</success_criteria>

<output>
Create `.planning/phases/03-download-page-redesign-dark-mode-complete/03-04-SUMMARY.md` when done.
</output>
