---
phase: 09-smoothui-foundation
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - design-system/MOTION.md
  - design-system/DESIGN-SYSTEM.md
autonomous: true
requirements: [DOC-01]
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "`design-system/MOTION.md` documents the shape presets, the transition/variant presets, and the reduced-motion behaviour — the recorded source of truth for the new SmoothUI shape + motion language (DOC-01)."
    - "MOTION.md explicitly states that colour tokens and Public Sans type tokens are unchanged (DOC-01)."
    - "`design-system/DESIGN-SYSTEM.md` points to MOTION.md so the new language is discoverable from the existing entry doc."
  artifacts:
    - design-system/MOTION.md
  key_links:
    - "design-system/MOTION.md preset tables ↔ src/lib/motion.ts + src/lib/shape.ts exports (the doc must match the code it documents)"
---

<objective>
Document the new SmoothUI shape + motion language in `design-system/`, as the recorded source of truth Phase 10/11 will follow, and add a pointer to it from the existing entry doc.

Purpose: DOC-01 — the shape/motion decisions must be written down and discoverable, and must state explicitly that colour and type tokens are untouched by this milestone's re-shape.
Output: `design-system/MOTION.md` (new) and a one-line pointer added to `design-system/DESIGN-SYSTEM.md`.

Scope guard: documentation only. Do NOT modify any token file, any runtime code, or the colour/type sections of DESIGN-SYSTEM.md.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-smoothui-foundation/09-CONTEXT.md
@.planning/phases/09-smoothui-foundation/09-RESEARCH.md
@design-system/DESIGN-SYSTEM.md
@src/lib/motion.ts
@src/lib/shape.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author design-system/MOTION.md and link it from DESIGN-SYSTEM.md (DOC-01)</name>
  <files>design-system/MOTION.md, design-system/DESIGN-SYSTEM.md</files>
  <read_first>
    - .planning/phases/09-smoothui-foundation/09-RESEARCH.md — "DOC-01: `design-system/MOTION.md` required content" (the required section skeleton), "Pattern 1/2/3" (the preset names/values to tabulate), "Pitfall 3" (document BOTH reduced-motion mechanisms)
    - src/lib/motion.ts — the AS-BUILT `transitions` and `variants` exports (the doc's tables must match these names/values, not the research sketch, in case they diverged during implementation)
    - src/lib/shape.ts — the AS-BUILT `shape` and `clipCorner` exports
    - design-system/DESIGN-SYSTEM.md — the existing "VISUAL FOUNDATIONS" doc; find where to add a short pointer to MOTION.md without restating colour/type
  </read_first>
  <action>
    Create `design-system/MOTION.md` documenting the shape + motion language as the recorded source of truth. It must contain: (1) an explicit opening statement that colour tokens (`design-system/tokens/colors.css`) and Public Sans type tokens are UNCHANGED by this document/milestone. (2) A Shape section — a table mapping each `shape` preset name → its Tailwind class → the underlying existing `--radius-*`/`--ring-*` token → typical usage, plus the `clipCorner` geometry constants (noting the compound component ships in Phase 10). (3) A Motion section — one table of transition presets (name → spring/tween config → typical usage) and one table of variants (name → full/reduced pair → typical usage). (4) A Reduced-motion section documenting BOTH required mechanisms (RESEARCH Pitfall 3): CSS-only interactions use the Tailwind `motion-reduce:` variant, and `motion/react`-driven animation goes through `useMotionPreset()`/`useReducedMotion()`, with `MotionConfig reducedMotion="user"` set at the app root. Populate every table from the AS-BUILT exports in `src/lib/motion.ts` and `src/lib/shape.ts` (read them first) so the doc matches the code; do not copy the research sketch blindly if implementation names/values differ.

    Add a single pointer line to `design-system/DESIGN-SYSTEM.md` (e.g. under VISUAL FOUNDATIONS) directing readers to MOTION.md for the shape + motion language. Do not restate colour or type values there and do not edit any other section.
  </action>
  <verify>
    <automated>test -f design-system/MOTION.md &amp;&amp; grep -qi "unchanged" design-system/MOTION.md &amp;&amp; grep -qi "reduced" design-system/MOTION.md &amp;&amp; grep -q "MOTION.md" design-system/DESIGN-SYSTEM.md &amp;&amp; grep -Eq "snappy|fill|micro|backdrop" design-system/MOTION.md &amp;&amp; grep -Eq "rounded|radius|ring" design-system/MOTION.md</automated>
    <fails_when>MOTION.md is missing; OR it does not state colour/type are unchanged; OR it omits the reduced-motion section; OR it omits the transition preset names or the shape/radii tables; OR DESIGN-SYSTEM.md has no pointer to MOTION.md.</fails_when>
  </verify>
  <acceptance_criteria>
    - `design-system/MOTION.md` exists with: an explicit "colour + Public Sans type tokens unchanged" statement; a Shape table (preset → Tailwind class → underlying token → usage) plus clipCorner constants; a Motion section with transition and variant tables; and a Reduced-motion section covering both the CSS `motion-reduce:` variant and the JS `useMotionPreset()`/`MotionConfig` path.
    - The MOTION.md tables reflect the AS-BUILT `src/lib/motion.ts` / `src/lib/shape.ts` exports.
    - `design-system/DESIGN-SYSTEM.md` contains a pointer to MOTION.md and no other section is changed.
  </acceptance_criteria>
  <done>The shape + motion language is documented as the recorded source of truth, explicitly notes colour/type are unchanged, and is discoverable from DESIGN-SYSTEM.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only plan — no trust boundary is introduced or crossed; no runtime code, tokens, input, network, or crypto path is touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02 | Tampering | `design-system/MOTION.md` content vs. as-built code | low | accept | The doc is descriptive, not executable, and carries no security control. Drift risk is mitigated by authoring the tables from the as-built `src/lib/motion.ts`/`src/lib/shape.ts` exports and by the verification grep that the required sections/preset names are present; no runtime or security impact. |
</threat_model>

<verification>
- `design-system/MOTION.md` exists and contains the four required sections (unchanged-colour/type statement, Shape, Motion, Reduced-motion).
- Preset names in the doc match the exported names in `src/lib/motion.ts` and `src/lib/shape.ts`.
- `design-system/DESIGN-SYSTEM.md` links to MOTION.md; git diff shows no change to any token file or to the colour/type sections.
</verification>

<success_criteria>
- DOC-01: `design-system/` documents the new SmoothUI shape + motion language (radii, borders, transition/variant presets, reduced-motion behaviour) and explicitly states colour/type tokens are unchanged, discoverable from the existing DESIGN-SYSTEM.md.
</success_criteria>

## Artifacts this phase produces (Plan 02)

| Artifact | Purpose | Requirement |
|----------|---------|-------------|
| `design-system/MOTION.md` | Recorded source of truth for the shape + motion language; explicit colour/type-unchanged statement | DOC-01 |
| `design-system/DESIGN-SYSTEM.md` (modified) | One-line pointer making MOTION.md discoverable | DOC-01 |

<output>
Create `.planning/phases/09-smoothui-foundation/09-02-SUMMARY.md` when done.
</output>
