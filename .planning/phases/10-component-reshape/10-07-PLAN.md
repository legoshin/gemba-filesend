---
phase: 10-component-reshape
plan: 07
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/file-dropzone.tsx
  - src/app/upload/page.tsx
  - src/app/download/page.tsx
  - src/components/ui/skeleton.tsx
autonomous: true
requirements: [FILE-01, FILE-02, FDBK-03]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The file-dropzone reacts to drag with SmoothUI motion and its selected-file rows animate on add/remove — multi-file selection and the client-side encryption-progress path are unchanged (FILE-01)"
    - "Share-result rows (upload) and the download file list use SmoothUI animated-list add/remove motion, edited surgically with no logic change (FILE-02)"
    - "A SmoothUI-style Skeleton loading treatment exists and is applied to the download loading state, degrading via motion-reduce (FDBK-03)"
    - "No crypto/storage/upload/download logic or value wiring changes — only presentational JSX"
  artifacts:
    - path: "src/components/file-dropzone.tsx"
      provides: "Drag-state motion on the dropzone + AnimatePresence/stagger selected-file rows (FileRow subcomponent for Rules-of-Hooks)"
      contains: "AnimatePresence"
    - path: "src/components/ui/skeleton.tsx"
      provides: "New SmoothUI skeleton — animate-pulse + shape.innerCard + motion-reduce:animate-none"
      contains: "animate-pulse"
    - path: "src/app/upload/page.tsx"
      provides: "Animated-list share-result rows (row subcomponent)"
      contains: "AnimatePresence"
    - path: "src/app/download/page.tsx"
      provides: "Animated-list file rows + Skeleton on the loading state"
      contains: "Skeleton"
  key_links:
    - from: "src/components/file-dropzone.tsx"
      to: "src/lib/motion.ts"
      via: "variants.stagger + transitions.snappy/micro for row + drag motion (presentational only)"
      pattern: "variants\\.stagger"
    - from: "src/app/download/page.tsx"
      to: "src/components/ui/skeleton.tsx"
      via: "Skeleton rendered in the metadata-loading state"
      pattern: "Skeleton"
---

<objective>
Reshape the file/list surfaces (FILE-01 file-dropzone, FILE-02 selected-file + share-result rows) onto SmoothUI animated-file-upload / animated-list motion, and create + apply the SmoothUI Skeleton loading treatment (FDBK-03). Every edit here is PRESENTATIONAL: the encryption/upload/download logic, drag/drop handlers, and live-progress value wiring are OFF LIMITS (CONTEXT.md encryption-boundary constraint; RESEARCH Pattern 3). This is the one plan that owns all Phase-10 page edits, consolidated to prevent wave conflicts.

Purpose: Give the file/list + loading surfaces SmoothUI motion without touching the client-side-encryption model.
Output: Reshaped file-dropzone, animated share/file-list rows, and a new Skeleton applied to the download loading state.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-component-reshape/10-RESEARCH.md
@.planning/phases/10-component-reshape/10-PATTERNS.md
@src/components/chip.tsx
@src/lib/motion.ts
@src/lib/shape.ts
@src/lib/use-motion-preset.ts
@src/components/file-dropzone.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reshape file-dropzone drag + selected-file rows, logic untouched (FILE-01)</name>
  <precondition>file-dropzone.tsx's handlers (handleDrag/handleDragIn/handleDragOut/handleDrop/handleFileSelect/filterBySize/removeFile/formatSize) and its isDragging/files state exist as-is — these are OFF LIMITS; only the JSX return changes.</precondition>
  <files>src/components/file-dropzone.tsx</files>
  <read_first>src/components/file-dropzone.tsx (full, ~180 lines — note the logic block lines ~20-109 is off-limits; the JSX return ~111-179 and the files.map at ~150 is the only edit surface), .planning/phases/10-component-reshape/10-RESEARCH.md (Pattern 3 full presentational diff + Rules-of-Hooks note re: FileRow subcomponent; Pitfall 4), src/components/chip.tsx (motion.create hoist), src/lib/motion.ts (variants.stagger, transitions.snappy/micro), src/lib/shape.ts (shape.card, shape.innerCard)</read_first>
  <action>Reshape ONLY the JSX return of `src/components/file-dropzone.tsx` (RESEARCH Pattern 3). Add the motion imports; hoist `const MotionLabel = motion.create("label")` at module scope. Replace the root `<label>` with `MotionLabel`, keeping ALL its existing `onDragEnter/onDragLeave/onDragOver/onDrop` handler bindings and className logic; add a drag-state scale via a direct `animate` off the existing `isDragging` state (Pattern 4 — resolve reduced motion with `useReducedMotion()` and the named `transitions.micro`, never an inline spring). For the selected-file rows: extract a `FileRow` subcomponent that calls `const rowMotion = useMotionPreset(variants.stagger, transitions.snappy)` ONCE per instance and renders `motion.div` with `layout` — this is REQUIRED, not optional (calling the hook inside `files.map(...)` violates Rules of Hooks — Pitfall 4). Wrap the `files.map(...)` in `<AnimatePresence initial={false}>` and map to `<FileRow key={\`${file.name}-${index}\`} .../>` so add/remove animates. Keep `shape.innerCard` on the rows and `shape.card` on the dropzone. DO NOT touch any handler, `filterBySize`, `removeFile`'s immutable filter, `formatSize`, or any `useState`/`useCallback` — the entire encryption-adjacent logic block stays byte-for-byte. Multi-file selection + the live encryption-progress must keep working. No colour/type token changes.</action>
  <verify>
    <automated>grep -q 'AnimatePresence' src/components/file-dropzone.tsx && grep -q 'variants.stagger' src/components/file-dropzone.tsx && grep -q 'motion.create' src/components/file-dropzone.tsx && npm run build && npm test && test -z "$(git status --porcelain -- src/lib/crypto.ts src/lib/storage.ts src/lib/server-storage.ts src/lib/blob-storage.ts src/lib/multi-file.ts design-system/tokens/)"</automated>
    <fails_when>file-dropzone lacks AnimatePresence + stagger rows + hoisted motion.create; OR `npm run build` fails; OR `npm test` drops below 123 passing (a logic regression); OR any crypto/storage/multi-file/token file was modified.</fails_when>
  </verify>
  <acceptance_criteria>
    - file-dropzone drag reacts via motion off the existing isDragging state; selected-file rows animate add/remove through AnimatePresence + a FileRow subcomponent using `useMotionPreset(variants.stagger, transitions.snappy)`.
    - No handler / filterBySize / removeFile / formatSize / state change; only the JSX return edited.
    - `npm run build` exits 0; `npm test` reports 123 passing; no crypto/storage/multi-file/token file changed.
  </acceptance_criteria>
  <done>The dropzone animates on drag and its file rows animate on add/remove, with multi-file selection + encryption progress fully intact.</done>
</task>

<task type="auto">
  <name>Task 2: Animated-list share-result + download file rows, surgical (FILE-02)</name>
  <precondition>The row-render call sites exist: src/app/upload/page.tsx `result.files.map(...)` (~line 551) and src/app/download/page.tsx `fileInfo.files.map(...)` (~line 601). Only these JSX map blocks change.</precondition>
  <files>src/app/upload/page.tsx, src/app/download/page.tsx</files>
  <read_first>src/app/upload/page.tsx around lines 540-575 (the result.files.map share-result rows — READ ONLY the render region), src/app/download/page.tsx around lines 590-625 (the fileInfo.files.map file rows), .planning/phases/10-component-reshape/10-RESEARCH.md (Component Map "List rows" + Pitfall 4 Rules-of-Hooks), src/lib/motion.ts (variants.stagger, transitions.snappy)</read_first>
  <action>Wrap ONLY the two identified `.map(...)` row-render blocks in SmoothUI animated-list motion. In `src/app/upload/page.tsx`, wrap the `result.files.map(...)` share-result list in `<AnimatePresence initial={false}>` and render each row via an extracted row subcomponent (e.g. `ShareResultRow`) that calls `useMotionPreset(variants.stagger, transitions.snappy)` once and renders `motion.div` with `layout` (Pitfall 4 — never call the hook inside the map). Do the same for `src/app/download/page.tsx`'s `fileInfo.files.map(...)` file list (e.g. a `DownloadFileRow` subcomponent). Change NOTHING else in either page — no upload/finalize/encryption state, no download/decrypt logic, no `<Progress value=...>` wiring, no data fetching, no other JSX. Preserve every existing class and the row keys. If a row subcomponent needs props currently closed over inline, pass them explicitly as props (do not lift or mutate page state). No colour/type token changes.</action>
  <verify>
    <automated>grep -q 'AnimatePresence' src/app/upload/page.tsx && grep -q 'AnimatePresence' src/app/download/page.tsx && npm run build && npm test && test -z "$(git status --porcelain -- src/lib/crypto.ts src/lib/storage.ts src/lib/server-storage.ts src/lib/blob-storage.ts src/lib/multi-file.ts design-system/tokens/)"</automated>
    <fails_when>Either page's row list is not wrapped in AnimatePresence; OR `npm run build` fails; OR `npm test` drops below 123 passing; OR a crypto/storage/multi-file/token file changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - upload share-result rows and download file rows render through AnimatePresence + a per-row subcomponent using `useMotionPreset(variants.stagger, transitions.snappy)` with `layout`.
    - Only the two `.map` render blocks changed; no encryption/upload/download/data-fetch/Progress-value logic touched.
    - `npm run build` exits 0; `npm test` reports 123 passing; no crypto/storage/multi-file/token file changed.
  </acceptance_criteria>
  <done>Share-result and download file rows carry SmoothUI animated-list add/remove motion, edited surgically with the encryption/transfer logic untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Create the Skeleton treatment + apply to the download loading state (FDBK-03)</name>
  <files>src/components/ui/skeleton.tsx, src/app/download/page.tsx</files>
  <read_first>src/app/download/page.tsx (locate the metadata-loading / fetching state that currently renders a spinner/placeholder — READ ONLY to find it), .planning/phases/10-component-reshape/10-RESEARCH.md (Component Map "Skeleton" — pure CSS animate-pulse, no motion.*, mechanism 1 reduced-motion), src/lib/shape.ts (shape.innerCard), src/components/ui/card.tsx (an existing ui/* leaf as a structural reference for the new file's shape/conventions)</read_first>
  <action>Create a new `src/components/ui/skeleton.tsx` following the codebase `ui/*` conventions (a `Skeleton` component typed via `React.ComponentProps<"div">`, `data-slot="skeleton"`, `cn()`-merged className). Implement it as a PURE CSS treatment (RESEARCH: SmoothUI's own skeleton uses `animate-pulse`, no `motion.*`): base classes `animate-pulse`, `shape.innerCard` geometry, a subdued surface background using existing semantic tokens (no new colour), and pair with `motion-reduce:animate-none` so reduced motion degrades it (mechanism 1 — Tailwind variant, no motion/react needed). Then apply it in `src/app/download/page.tsx`'s loading state: replace/augment the current loading placeholder with `<Skeleton>` block(s) matching the metadata layout. Keep the change scoped to the loading-state JSX only — no data-fetch/decrypt/download logic change. No colour/type token changes.</action>
  <verify>
    <automated>test -f src/components/ui/skeleton.tsx && grep -q 'animate-pulse' src/components/ui/skeleton.tsx && grep -q 'motion-reduce:animate-none' src/components/ui/skeleton.tsx && grep -q 'Skeleton' src/app/download/page.tsx && npm run build && npm test && test -z "$(git status --porcelain -- src/lib/crypto.ts src/lib/storage.ts design-system/tokens/)"</automated>
    <fails_when>skeleton.tsx is missing or lacks animate-pulse / motion-reduce:animate-none; OR it is not rendered in the download loading state; OR `npm run build` fails; OR `npm test` drops below 123 passing; OR a crypto/storage/token file changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - src/components/ui/skeleton.tsx exists: a pure-CSS `Skeleton` (animate-pulse + shape.innerCard + motion-reduce:animate-none), no motion/react import.
    - It is applied to the download page's loading state (loading-state JSX only).
    - `npm run build` exits 0; `npm test` reports 123 passing; no crypto/storage/token file changed.
  </acceptance_criteria>
  <done>A SmoothUI Skeleton treatment exists, degrades under reduced motion via motion-reduce, and shows on the download loading state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side encryption/upload/download state → presentational JSX | This plan edits page/component JSX that sits ADJACENT to the encryption boundary. The AES key (URL fragment) and all crypto/transfer logic must stay entirely out of the edit surface. |
| user (browser) → file-dropzone | Existing drag/drop + file validation (filterBySize) is untouched; motion wraps presentation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07-01 | Tampering | a presentational edit accidentally altering encryption/upload/download logic or the Progress value wiring | high | mitigate | JSX-only edits; git-diff guard asserts crypto/storage/multi-file libs unmodified; `npm test` (123 tests incl. crypto round-trip, storage, multi-file) must stay green; precondition pins the exact edit surface. |
| T-10-07-02 | Information Disclosure | a row subcomponent lifting/leaking encryption state via new props | medium | mitigate | Pass only presentational row data as explicit props; no page-state mutation; review + build + tests. |
| T-10-07-03 | DoS (UX) | Rules-of-Hooks violation crashing on file add/remove | medium | mitigate | Required FileRow/ShareResultRow/DownloadFileRow subcomponent extraction (Pitfall 4); build catches the hook-order error surface. |

No package installs — supply-chain not applicable.
</threat_model>

<verification>
- `npm run build` exits 0.
- `npm test` still reports 123 passing (crypto/storage/multi-file logic unregressed — the primary encryption-boundary guard).
- AnimatePresence/stagger present in file-dropzone + both pages; Skeleton exists (pure CSS) and is used on the download loading state.
- `git status --porcelain` shows NO changes under src/lib/crypto.ts, storage.ts, server-storage.ts, blob-storage.ts, multi-file.ts, or design-system/tokens/.
</verification>

<success_criteria>
File-dropzone drag + selected-file rows, share-result rows, and download file rows carry SmoothUI animated-list motion; a Skeleton treatment exists and shows on loading — all presentational, with the client-side encryption boundary and multi-file behaviour provably unweakened (123 tests green, crypto/storage libs untouched).
</success_criteria>

## Artifacts this phase produces (this plan)
- `src/components/file-dropzone.tsx` — reshaped drag + animated file rows.
- `src/components/ui/skeleton.tsx` — new SmoothUI skeleton treatment.
- `src/app/upload/page.tsx`, `src/app/download/page.tsx` — surgical animated-list row wrapping + skeleton loading state.

<output>
Create `.planning/phases/10-component-reshape/10-07-SUMMARY.md` when done.
</output>
