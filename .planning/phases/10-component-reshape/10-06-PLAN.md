---
phase: 10-component-reshape
plan: 06
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/ui/tabs.tsx
  - src/components/app-shell.tsx
  - src/components/mobile-tab-bar.tsx
  - src/components/theme-toggle.tsx
autonomous: true
requirements: [SURF-03, SHELL-01, SHELL-02]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Tabs shows a smoothly-sliding active indicator via layoutId, preserving Radix selection behaviour (SURF-03)"
    - "The desktop sidebar and mobile tab bar show a sliding active-link indicator driven by the existing usePathname() active state, with nav/embed logic untouched (SHELL-01)"
    - "Theme-toggle crossfades its trigger icon on theme change while keeping the 3-way light/dark/system DropdownMenuRadioGroup intact (SHELL-02)"
    - "All indicators/icons degrade under prefers-reduced-motion"
  artifacts:
    - path: "src/components/ui/tabs.tsx"
      provides: "layoutId sliding active-tab indicator (replaces the CSS after: opacity toggle)"
      contains: "layoutId"
    - path: "src/components/app-shell.tsx"
      provides: "Sliding active-link indicator on the desktop sidebar nav"
      contains: "layoutId"
    - path: "src/components/mobile-tab-bar.tsx"
      provides: "Sliding active-tab indicator on the mobile bottom nav"
      contains: "layoutId"
    - path: "src/components/theme-toggle.tsx"
      provides: "AnimatePresence icon crossfade keyed on resolvedTheme; 3-way control preserved"
      contains: "AnimatePresence"
  key_links:
    - from: "src/components/theme-toggle.tsx"
      to: "src/components/ui/dropdown-menu.tsx"
      via: "renders the reshaped DropdownMenuContent (10-05) — inherits panel motion; theme-toggle only adds its own icon crossfade"
      pattern: "DropdownMenu"
    - from: "src/components/mobile-tab-bar.tsx"
      to: "src/components/app-shell.tsx"
      via: "sibling nav — same layoutId active-indicator technique, mirrored"
      pattern: "layoutId"
---

<objective>
Reshape the sliding-indicator family — Tabs (SURF-03) and the app-shell sidebar + mobile-tab-bar nav (SHELL-01) all use the same `layoutId` active-indicator technique (RESEARCH: reuse `animated-tabs`'s pattern, NOT SmoothUI's `dock`) — plus the theme-toggle icon crossfade (SHELL-02). Radix keeps tab selection; `usePathname()` keeps nav routing; the 3-way theme control stays intact.

Purpose: One cohesive wave for every layoutId/nav motion surface plus the theme-toggle icon.
Output: Reshaped Tabs, app-shell nav, mobile-tab-bar, theme-toggle.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-component-reshape/10-RESEARCH.md
@.planning/phases/10-component-reshape/10-PATTERNS.md
@src/lib/motion.ts
@src/lib/use-motion-preset.ts
@src/components/ui/tabs.tsx
@src/components/app-shell.tsx
@src/components/mobile-tab-bar.tsx
@src/components/theme-toggle.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reshape Tabs with a layoutId sliding indicator (SURF-03)</name>
  <files>src/components/ui/tabs.tsx</files>
  <read_first>src/components/ui/tabs.tsx (full, ~90 lines — current `after:` pseudo-element opacity-toggle indicator on TabsTrigger), .planning/phases/10-component-reshape/10-RESEARCH.md (Code Examples "Tabs" + Open Question #1: layoutId-per-active-trigger vs. sibling-indicator; Assumption A1: the choice is contained to tabs.tsx alone), src/lib/motion.ts (transitions.snappy)</read_first>
  <action>Reshape `src/components/ui/tabs.tsx` to a `layoutId`-driven sliding active indicator, replacing the static `after:opacity-0`/`data-[state=active]:after:opacity-100` toggle. Per RESEARCH Open Question #1, implement the LOWER-RISK shadcn-community approach the research names: an absolutely-positioned `motion.span` indicator sharing a single `layoutId` (e.g. `"tabs-active-indicator"`), rendered for the active trigger and animated with `transition={transitions.snappy}` (a layoutId element needs only a transition, no variants pair). Tabs has ZERO live consumers today, so this is safe to build without regressing a page; verify the reshape compiles and the indicator wiring is present. Keep `TabsPrimitive.Root`/`List`/`Trigger`/`Content` behaviour, `value`/`data-state`, and keyboard selection unchanged — Radix still owns which tab is active; the indicator only reads `data-state="active"`. Keep the file `"use client"`. If the chosen approach proves visually wrong, the fix is contained to this file (reversible). No colour/type/radius value changes.</action>
  <verify>
    <automated>grep -q 'layoutId' src/components/ui/tabs.tsx && grep -q 'motion' src/components/ui/tabs.tsx && grep -Eq 'transitions\.snappy|snappy' src/components/ui/tabs.tsx && npm run build && test -z "$(git status --porcelain -- design-system/tokens/)"</automated>
    <fails_when>Tabs lacks a layoutId motion indicator referencing the named snappy transition, OR `npm run build` errors, OR a token file changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - tabs.tsx renders a `layoutId` `motion.span` active indicator using `transitions.snappy`; the CSS `after:` opacity-toggle indicator is superseded.
    - Radix Root/List/Trigger/Content + value/data-state + keyboard selection preserved.
    - `npm run build` exits 0; no token file changed.
  </acceptance_criteria>
  <done>The active tab indicator slides smoothly via layoutId while Radix keeps selection + keyboard a11y.</done>
</task>

<task type="auto">
  <name>Task 2: Sliding active-link indicator on sidebar + mobile tab bar (SHELL-01)</name>
  <files>src/components/app-shell.tsx, src/components/mobile-tab-bar.tsx</files>
  <read_first>src/components/app-shell.tsx (full, ~80 lines — navLinks array, usePathname() active boolean, active bg swap; also renders ThemeToggle + MobileTabBar; contains embed-mode logic), src/components/mobile-tab-bar.tsx (full, ~40 lines — sibling tabs array + usePathname()), .planning/phases/10-component-reshape/10-RESEARCH.md (Component Map "AppShell nav" + Common Pitfall 6: NOT dock), .planning/phases/10-component-reshape/10-PATTERNS.md (Group J), src/lib/motion.ts (transitions.snappy)</read_first>
  <action>Reshape the active-link indicator in BOTH `src/components/app-shell.tsx` (desktop sidebar nav) and `src/components/mobile-tab-bar.tsx` (mobile bottom nav) using the SAME `layoutId` technique as Task 1 (RESEARCH: reuse the animated-tabs pattern; do NOT import dock/floating-navbar physics — Pitfall 6). Keep the existing `usePathname()`-driven `active` boolean, the `navLinks`/`tabs` arrays, all routing `<Link>`s, and — critically — the app-shell embed-mode logic and layout structure UNCHANGED. Behind the active link, render a `motion.span` indicator (the active background pill) with a shared `layoutId` per surface (e.g. `"sidebar-nav-active"` for the sidebar, a separate `"mobile-nav-active"` for the tab bar — they are distinct navs, so distinct layoutIds), `transition={transitions.snappy}`, so the active pill slides between links on route change. Preserve the active/inactive text + hover classes; the indicator sits behind the label/icon (z-order). Add `"use client"` where a file is not already client (both already use hooks). No colour/type/radius value changes; no navigation logic change.</action>
  <verify>
    <automated>grep -q 'layoutId' src/components/app-shell.tsx && grep -q 'layoutId' src/components/mobile-tab-bar.tsx && grep -q 'usePathname' src/components/app-shell.tsx && npm run build && test -z "$(git status --porcelain -- design-system/tokens/)"</automated>
    <fails_when>Either nav file lacks a layoutId indicator, OR usePathname routing was removed from app-shell, OR `npm run build` errors (app-shell renders on every route), OR a token file changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both app-shell sidebar and mobile-tab-bar render a `layoutId` `motion.span` active indicator with `transitions.snappy`.
    - `usePathname()` active logic, navLinks/tabs arrays, routing Links, and embed-mode logic are unchanged.
    - No dock/floating-navbar physics imported.
    - `npm run build` exits 0; no token file changed.
  </acceptance_criteria>
  <done>The desktop sidebar and mobile tab bar show a sliding active-link indicator on route change, with all nav/embed logic intact.</done>
</task>

<task type="auto">
  <name>Task 3: Reshape theme-toggle icon crossfade, keep 3-way control (SHELL-02)</name>
  <files>src/components/theme-toggle.tsx</files>
  <read_first>src/components/theme-toggle.tsx (full, ~50 lines — current both-icons-in-DOM CSS `dark:` visibility toggle; DropdownMenuRadioGroup 3-way), .planning/phases/10-component-reshape/10-RESEARCH.md (Code Examples "ThemeToggle" — AnimatePresence mode="wait" keyed on resolvedTheme; State-of-the-Art note: DOM shape changes from both-icons to one-swapped-via-key), src/lib/motion.ts (variants.scaleIn, transitions.snappy)</read_first>
  <action>Reshape `src/components/theme-toggle.tsx`'s trigger icon per RESEARCH Code Examples "ThemeToggle": replace the current both-icons-CSS-`dark:`-toggle with a single icon rendered inside `<AnimatePresence mode="wait" initial={false}>`, a `motion.span key={resolvedTheme}` spreading `useMotionPreset(variants.scaleIn, transitions.snappy)`, showing the Sun / Moon01 glyph based on `resolvedTheme` (NOT `theme` — `theme` can be "system" which has no icon; `resolvedTheme` is always light|dark). Keep the DropdownMenu-based 3-way control EXACTLY: the `DropdownMenuRadioGroup value={theme} onValueChange={setTheme}` with its light/dark/system `DropdownMenuRadioItem`s stays intact (SHELL-02 hard constraint). The DropdownMenuContent panel motion is inherited automatically from the 10-05 dropdown-menu reshape — do not re-implement it here. Keep the trigger `Button` (inherits 10-01's reshape). No colour/type token changes.</action>
  <verify>
    <automated>grep -q 'AnimatePresence' src/components/theme-toggle.tsx && grep -q 'resolvedTheme' src/components/theme-toggle.tsx && grep -q 'DropdownMenuRadioGroup' src/components/theme-toggle.tsx && npm run build && test -z "$(git status --porcelain -- design-system/tokens/)"</automated>
    <fails_when>theme-toggle lacks the AnimatePresence icon crossfade keyed on resolvedTheme, OR the 3-way DropdownMenuRadioGroup was removed, OR `npm run build` errors, OR a token file changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - theme-toggle crossfades a single `resolvedTheme`-keyed icon via AnimatePresence + `useMotionPreset(variants.scaleIn, transitions.snappy)`.
    - The 3-way light/dark/system DropdownMenuRadioGroup (value=theme, onValueChange=setTheme) is preserved.
    - `npm run build` exits 0; no token file changed.
  </acceptance_criteria>
  <done>The theme-toggle trigger icon crossfades on theme change while the 3-way light/dark/system control stays fully intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user (browser) → nav/toggle client components | All four are presentational client components; routing state (usePathname) and theme state (next-themes) are existing, untouched; no server, no new input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06-01 | Tampering (UX regression) | app-shell reshape breaking nav/embed logic (renders on every route) | medium | mitigate | Indicator-only visual edit; usePathname/navLinks/embed logic preserved; `npm run build` compiles every route. |
| T-10-06-02 | Tampering | theme-toggle DOM-shape change (both-icons → one-key-swap) breaking the 3-way control | low | mitigate | grep guard asserts DropdownMenuRadioGroup preserved; RESEARCH confirms no code queries the toggle's DOM structure. |
| T-10-06-03 | DoS (UX) | reduced-motion not honored on layoutId/icon motion | low | mitigate | Motion resolved via useMotionPreset / named snappy; app-root MotionConfig belt-and-suspenders. |

No package installs — supply-chain not applicable.
</threat_model>

<verification>
- `npm run build` exits 0.
- layoutId indicators present in tabs/app-shell/mobile-tab-bar; theme-toggle keeps AnimatePresence(resolvedTheme) + DropdownMenuRadioGroup.
- `git status --porcelain -- design-system/tokens/` empty.
- Regression: `npm test` still 123 passing.
</verification>

<success_criteria>
Tabs, sidebar, and mobile tab bar show sliding layoutId active indicators with all selection/routing/embed logic intact; theme-toggle crossfades its icon while keeping the 3-way control; reduced motion honored.
</success_criteria>

## Artifacts this phase produces (this plan)
- `src/components/ui/tabs.tsx`, `src/components/app-shell.tsx`, `src/components/mobile-tab-bar.tsx`, `src/components/theme-toggle.tsx` — reshaped indicator/nav/toggle surfaces.

<output>
Create `.planning/phases/10-component-reshape/10-06-SUMMARY.md` when done.
</output>
