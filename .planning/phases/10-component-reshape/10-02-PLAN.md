---
phase: 10-component-reshape
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/components/ui/checkbox.tsx
  - src/components/ui/radio-group.tsx
  - src/components/ui/switch.tsx
autonomous: true
requirements: [FORM-01, FORM-02, FORM-03, FORM-04]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The text Input reshapes to SmoothUI geometry with a focus pop, preserving its native/validation behaviour (FORM-01)"
    - "Checkbox and RadioGroup animate their check/dot on select while Radix owns state, keyboard, and aria (FORM-02)"
    - "The Switch thumb travels with a spring instead of a CSS ease, preserving Radix state + a11y (FORM-03)"
    - "Label + field grouping shape/spacing/focus ring stay consistent with the reshaped controls (FORM-04)"
    - "Every animated control degrades under prefers-reduced-motion via the Phase 9 presets"
  artifacts:
    - path: "src/components/ui/input.tsx"
      provides: "SmoothUI Input — focus pop via variants.focusPop, native input + validation preserved"
      contains: "useMotionPreset"
    - path: "src/components/ui/checkbox.tsx"
      provides: "Animated check via forceMount indicator + AnimatePresence + motion CheckIcon"
      contains: "AnimatePresence"
    - path: "src/components/ui/radio-group.tsx"
      provides: "Animated dot via motion.span scale-in"
      contains: "useMotionPreset"
    - path: "src/components/ui/switch.tsx"
      provides: "Spring-driven thumb travel off data-state"
      contains: "motion"
    - path: "src/components/ui/label.tsx"
      provides: "Shape/spacing aligned with reshaped controls"
  key_links:
    - from: "src/components/ui/input.tsx"
      to: "src/lib/motion.ts"
      via: "useMotionPreset(variants.focusPop, transitions.micro) — the foundation addition from 10-01"
      pattern: "focusPop"
    - from: "src/components/ui/checkbox.tsx"
      to: "src/lib/use-motion-preset.ts"
      via: "useMotionPreset(variants.scaleIn, transitions.snappy) drives the check reveal inside AnimatePresence"
      pattern: "useMotionPreset"
---

<objective>
Reshape the form-control group (FORM-01..04) onto SmoothUI geometry + motion in place on their existing Radix primitives / native elements, consuming the Phase 9 foundation and 10-01's new `variants.focusPop`. Radix keeps all state, keyboard interaction, focus management, and ARIA; motion only decorates the visual layer.

Purpose: Bring the whole forms family up to the `chip.tsx`/foundation pattern in one cohesive wave.
Output: Reshaped Input, Checkbox, RadioGroup, Switch, and a shape/spacing-aligned Label.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-component-reshape/10-RESEARCH.md
@.planning/phases/10-component-reshape/10-PATTERNS.md
@src/components/chip.tsx
@src/lib/motion.ts
@src/lib/shape.ts
@src/lib/use-motion-preset.ts
@src/components/ui/input.tsx
@src/components/ui/label.tsx
@src/components/ui/checkbox.tsx
@src/components/ui/radio-group.tsx
@src/components/ui/switch.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reshape Input (FORM-01) with focusPop + align Label (FORM-04)</name>
  <files>src/components/ui/input.tsx, src/components/ui/label.tsx</files>
  <read_first>src/components/ui/input.tsx (full, 21 lines — plain native input, no "use client"), src/components/ui/label.tsx (full, 24 lines), src/lib/motion.ts (variants.focusPop added in 10-01, transitions.micro), src/lib/shape.ts (shape.field == rounded-[var(--radius-sm)], shape.ringFocus), .planning/phases/10-component-reshape/10-RESEARCH.md (Component Map "Input" row + anti-pattern "Adopting SmoothUI's floating-label markup" — do NOT do that)</read_first>
  <behavior>
    - On focus, Input applies a subtle scale pop from variants.focusPop.full; under reduced motion (focusPop.reduced.whileFocus == {}) it applies no scale.
    - The element stays a real native input: type, value/defaultValue, ref forwarding, autocomplete/autofill, aria-invalid, and data-slot="input" all behave exactly as before.
  </behavior>
  <action>Reshape `src/components/ui/input.tsx`: add `"use client"` and imports (`motion` from "motion/react"; `transitions, variants` from "@/lib/motion"; `useMotionPreset` from "@/lib/use-motion-preset"). Hoist `const MotionInput = motion.create("input")` at module scope (never in render). Render `MotionInput` in place of `<input>`, keeping the SAME `type`, `data-slot="input"`, and the entire existing className string unchanged (it already uses `rounded-[var(--radius-sm)]` = `shape.field` and `focus-visible:shadow-[var(--ring-focus)]` = `shape.ringFocus`; leave those literals or swap to the `shape.*` constants — either is a drop-in, make NO radius/colour value change). Resolve `const focus = useMotionPreset(variants.focusPop, transitions.micro)` and spread `{...focus}` so `whileFocus` drives the pop. Do NOT adopt SmoothUI's floating-label markup (RESEARCH anti-pattern — it breaks Gemba's separate Label + htmlFor association used by every other control). For `src/components/ui/label.tsx` (FORM-04): keep it a plain shape-only leaf (no motion) — confirm its spacing/gap and any focus-ring styling stay consistent with the reshaped controls; if it hardcodes a radius or spacing value that diverges from the control tokens, align it to the matching `shape.*`/spacing token, otherwise leave it byte-for-byte.</action>
  <verify>
    <automated>grep -q '"use client"' src/components/ui/input.tsx && grep -q 'motion.create' src/components/ui/input.tsx && grep -q 'focusPop' src/components/ui/input.tsx && npm run build && test -z "$(git status --porcelain -- design-system/tokens/)"</automated>
    <fails_when>Input lacks the client directive / hoisted motion.create / focusPop wiring; OR `npm run build` errors (Input is consumed by upload, download, and recipient-chip-input pages); OR a colour/type token file changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - input.tsx is a client component rendering a hoisted `motion.create("input")` with `whileFocus` from `useMotionPreset(variants.focusPop, transitions.micro)`.
    - Input stays a native input (type/data-slot/validation classes/aria-invalid unchanged); no floating-label markup introduced.
    - label.tsx carries no motion and its shape/spacing matches the controls (no divergent hardcoded radius/spacing).
    - `npm run build` exits 0; no token file changed.
  </acceptance_criteria>
  <done>Input pops subtly on focus (or not, under reduced motion) via the shared focusPop preset while staying a fully-native, validation-preserving input; Label geometry stays consistent with it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reshape Checkbox + RadioGroup with check/dot motion (FORM-02)</name>
  <files>src/components/ui/checkbox.tsx, src/components/ui/radio-group.tsx</files>
  <read_first>src/components/ui/checkbox.tsx (full), src/components/ui/radio-group.tsx (full), .planning/phases/10-component-reshape/10-RESEARCH.md (Code Examples "Checkbox — check-draw" + note about forceMount on the Indicator; Common Pitfall/Open-Question #3 re: forceMount a11y), src/components/chip.tsx (motion.create hoist pattern), src/lib/motion.ts (variants.scaleIn, transitions.snappy)</read_first>
  <behavior>
    - Checking a box animates the CheckIcon in (scale/opacity via variants.scaleIn); unchecking animates it out via AnimatePresence exit. Radix's aria-checked/data-state on the Root are unchanged.
    - Selecting a radio animates the inner dot in via a motion.span scale-in; Radix keyboard arrow-navigation and roving tabindex are unchanged.
    - Under reduced motion both degrade to opacity-only via the presets.
  </behavior>
  <action>Checkbox (`src/components/ui/checkbox.tsx`): follow RESEARCH Code Examples "Checkbox" — hoist `const MotionCheckIcon = motion.create(CheckIcon)` at module scope; keep `CheckboxPrimitive.Root` and its className byte-for-byte (Radix owns aria-checked/data-state on the Root). Add `forceMount` to `CheckboxPrimitive.Indicator` and wrap the icon in `<AnimatePresence mode="wait">` rendering `MotionCheckIcon` only when checked, with `{...useMotionPreset(variants.scaleIn, transitions.snappy)}` resolved once in the component body (NOT inside a map). `forceMount` is required so Radix keeps the indicator mounted through the exit (RESEARCH note). RadioGroup (`src/components/ui/radio-group.tsx`): keep `RadioGroupPrimitive.Root`/`.Item`/`.Indicator` and all `data-[state=checked]` styling; replace the static dot inside `.Indicator` with a `motion.span` (or hoisted `motion.create`) spreading `useMotionPreset(variants.scaleIn, transitions.snappy)` so the dot scales in. Do NOT introduce `forwardRef` or `displayName` (this codebase uses React 19 ref-as-prop and sets neither — PATTERNS Group B convention); keep the `React.ComponentProps<typeof XPrimitive.Root>` prop typing. No colour/type/radius value changes.</action>
  <verify>
    <automated>grep -q 'AnimatePresence' src/components/ui/checkbox.tsx && grep -q 'forceMount' src/components/ui/checkbox.tsx && grep -q 'useMotionPreset' src/components/ui/checkbox.tsx && grep -q 'useMotionPreset' src/components/ui/radio-group.tsx && npm run build && test -z "$(git status --porcelain -- design-system/tokens/)"</automated>
    <fails_when>Checkbox lacks AnimatePresence + forceMount + useMotionPreset, OR RadioGroup lacks the dot motion wiring, OR `npm run build` errors, OR a token file changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - Checkbox check reveal/exit runs through AnimatePresence + a forceMount indicator + `motion.create(CheckIcon)`, resolved via useMotionPreset (no per-map hook calls).
    - RadioGroup dot scales in via a motion element + useMotionPreset(variants.scaleIn).
    - No forwardRef/displayName added; Radix Root/Item/Indicator + aria/data-state styling preserved.
    - `npm run build` exits 0; no token file changed.
  </acceptance_criteria>
  <done>Checkbox and RadioGroup animate their selection while Radix keeps full a11y/keyboard behaviour and aria-checked correctness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reshape Switch thumb with a spring (FORM-03)</name>
  <files>src/components/ui/switch.tsx</files>
  <read_first>src/components/ui/switch.tsx (full — current CSS `transition-transform` + `translate-x` thumb), .planning/phases/10-component-reshape/10-RESEARCH.md (Component Map "Switch" — Pattern 4 direct state-driven animate), src/lib/motion.ts (transitions.snappy)</read_first>
  <behavior>
    - Toggling the switch springs the thumb between the unchecked and checked x positions (transitions.snappy) instead of the current CSS ease; Radix owns checked state, aria-checked, and keyboard toggle.
    - Under reduced motion the thumb still moves to the correct position but without the spring (resolved via the reduced-motion path).
  </behavior>
  <action>Reshape `src/components/ui/switch.tsx`: keep `SwitchPrimitive.Root` and its className (all `data-[state=checked]`/`data-[state=unchecked]` styling) unchanged. Replace `SwitchPrimitive.Thumb`'s CSS `transition-transform`/`translate-x` travel (Pattern 4 — a two-state, not enter/exit, animation) with a `motion` thumb (hoist `motion.create(SwitchPrimitive.Thumb)` at module scope, or wrap the thumb in `motion.span`) that animates its `x` directly off Radix's `data-state`, using `transition={transitions.snappy}`. Because this is a direct two-state animate (not an AnimatePresence enter/exit), resolve reduced motion with `useReducedMotion()` from motion/react (or route the transition through the reduced path) so the spring collapses when the user opts out — never inline a raw spring object; use the named `transitions.snappy` constant. Keep the file `"use client"` (it already is). No colour/type/radius value changes.</action>
  <verify>
    <automated>grep -q 'motion' src/components/ui/switch.tsx && grep -q 'transitions.snappy\|transitions\.snappy\|snappy' src/components/ui/switch.tsx && npm run build && test -z "$(git status --porcelain -- design-system/tokens/)"</automated>
    <fails_when>Switch thumb is not driven by a motion element referencing the named snappy transition, OR `npm run build` errors, OR a token file changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - Switch thumb travel is a motion animation off data-state using the named `transitions.snappy` (no inlined spring literals).
    - Reduced motion collapses the spring to an instant move.
    - Radix Root state/aria-checked/keyboard preserved.
    - `npm run build` exits 0; no token file changed.
  </acceptance_criteria>
  <done>The Switch thumb travels with the SmoothUI spring while Radix keeps state + a11y, and it degrades under reduced motion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user (browser) → client control | All four controls are presentational client components wrapping Radix/native inputs; no server, no new input handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02-01 | Information Disclosure | Checkbox forceMount making the indicator wrapper always-present | low | accept | aria-checked/data-state live on the Radix Root, unaffected by forceMount on the Indicator (RESEARCH Security Domain); no existing test asserts indicator DOM presence (lib-only test suite). |
| T-10-02-02 | Tampering (UX regression) | Input reshape breaking upload/download/recipient-chip-input | low | mitigate | `npm run build` compiles every Input call site before merge. |
| T-10-02-03 | DoS (UX) | reduced-motion not honored | low | mitigate | All motion resolved through the Phase 9 presets / useReducedMotion. |

No package installs — supply-chain not applicable.
</threat_model>

<verification>
- `npm run build` exits 0.
- Motion wiring present in input/checkbox/radio-group/switch (greps above).
- `git status --porcelain -- design-system/tokens/` empty.
- Regression: `npm test` still reports 123 passing.
</verification>

<success_criteria>
All four form controls render SmoothUI geometry + focus/check/select/toggle motion, preserve Radix behaviour/keyboard/aria/validation, and degrade under reduced motion; Label geometry stays consistent.
</success_criteria>

## Artifacts this phase produces (this plan)
- `src/components/ui/input.tsx`, `label.tsx`, `checkbox.tsx`, `radio-group.tsx`, `switch.tsx` — reshaped form controls.

<output>
Create `.planning/phases/10-component-reshape/10-02-SUMMARY.md` when done.
</output>
