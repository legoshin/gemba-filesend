---
phase: 01-design-foundation-home-page
plan: 6
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - src/app/page.tsx
autonomous: true
requirements: [PAGE-01]
must_haves:
  truths:
    - "The home page reads as an app landing: a dominant 'Send a file' primary CTA is the focal point, with a secondary 'Receive a file' and a tighter feature/trust row (D-05)"
    - "The hero uses flat .gemba-h1 text on a flat surface — no gradient text, no radial-gradient background (D-05 guardrail)"
    - "Trust badges render via the Chip component; feature cards use the Card recipe; all icons render via the Icon wrapper (COMP-03/05, COMP-04)"
    - "The page renders correctly in light and dark using only Gemba token-backed utilities (PAGE-01)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Redesigned Gemba home page (app landing)"
      min_lines: 40
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/chip.tsx + src/components/ui/card.tsx + src/components/ui/button.tsx + src/components/icon.tsx"
      via: "home page composed from the shared component layer"
      pattern: "from\\s+[\"']@/components/chip[\"']"
---

<objective>
Redesign the home page to the Gemba design system as a streamlined app landing (D-05, PAGE-01): lead with the dominant "Send a file" primary CTA, keep a secondary "Receive a file", tighten the feature/trust row, and DROP the gradient hero and the how-it-works / repeated-CTA sections. Everything is composed from the shared component layer (Button ranks, Card recipe, Chip, Icon) and painted from Gemba tokens so it is correct in light and dark.

Purpose: Satisfy PAGE-01 (home page redesigned to the design system) and its dark-mode correctness.
Output: A rewritten `src/app/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-design-foundation-home-page/01-UI-SPEC.md
@.planning/phases/01-design-foundation-home-page/01-PATTERNS.md
@.planning/phases/01-design-foundation-home-page/01-CONTEXT.md
@design-system/APPLY-GUIDE.md
@src/app/page.tsx
@src/components/ui/button.tsx
@src/components/ui/card.tsx
@src/components/chip.tsx
@src/components/icon.tsx
---
Note: Button (01-03), Card (01-03), Chip (01-03) and Icon (01-02) are consumed here as-is. The page renders inside AppShell (01-05) — do not add shell/nav chrome to page.tsx.
</context>

## Artifacts this phase produces (this plan)

- `src/app/page.tsx` — redesigned home page: hero (flat `.gemba-h1` headline + subcopy), the "Send a file" / "Receive a file" CTA pair, trust `Chip`s, and a tightened feature-card row using the Card recipe + Icon tiles. No gradients, no how-it-works/CTA-repeat sections, no emoji, no lucide.

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the hero + CTA pair (focal point) and trust chips (D-05)</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current hero lines 70-105: radial-gradient bg line 72, gradient text lines 79-83, Badge lines 75-77, CTA links lines 89-101 — all to be replaced)
    - 01-UI-SPEC.md "Copywriting Contract" (focal point = "Send a file" primary CTA; hierarchy CTA → headline → secondary → feature row) + "Color" + "Typography"
    - 01-CONTEXT.md D-05 (app landing, remove gradients, drop how-it-works/CTA repetition)
    - src/components/ui/button.tsx (ranks/sizes), src/components/chip.tsx (neutral/accent), src/components/icon.tsx
  </read_first>
  <action>
    Replace the hero section. DELETE the `absolute ... bg-[radial-gradient(...)]` background div (line 72) and the gradient-text span (`bg-gradient-to-r ... bg-clip-text text-transparent`, lines 79-83) — hard guardrails (no gradients). Rebuild as a flat, centered hero on the shell's `--surface-page`: a flat `.gemba-h1` headline in sentence case (plain ink text, no gradient span), a `.gemba-body` subdued subcopy line, then the CTA pair as the visual focal point. Primary CTA: `<Link href="/upload"><Button size="default">Send a file</Button></Link>` — the solid ink Primary rank pill is the single dominant focal point (verb-first, replaces "Start Uploading"). Secondary CTA: `<Link href="/download"><Button variant="secondary" size="default">Receive a file</Button></Link>` (replaces "Download a File"). Icons via the `<Icon>` wrapper only (e.g. an upload glyph in the primary button, a download/arrow glyph in the secondary) — confirm glyph names against icon-data.js; NO lucide, NO emoji. Place the two trust badges as `<Chip variant="neutral">OPEN SOURCE</Chip>` and `<Chip variant="accent">END-TO-END ENCRYPTED</Chip>` (ALL-CAPS), replacing the old gradient `Badge`. Use `.gemba-*` helper classes / Gemba token utilities only — no raw hex, no gradient utilities.
  </action>
  <verify>
    <automated>! grep -qE "gradient|bg-clip-text|text-transparent" src/app/page.tsx && ! grep -q "lucide-react" src/app/page.tsx && grep -q "Send a file" src/app/page.tsx && grep -q "Receive a file" src/app/page.tsx && grep -qE "from\s+[\"']@/components/chip[\"']" src/app/page.tsx && grep -qE "gemba-h1" src/app/page.tsx && npx tsc --noEmit 2>&1 | tail -3 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - No `gradient`, `bg-clip-text`, or `text-transparent` remains in page.tsx; the radial-gradient background div is gone.
    - The hero headline uses `.gemba-h1` (flat ink text); subcopy uses a Gemba body class.
    - Primary CTA copy is "Send a file" (ink Primary pill, links to /upload); secondary CTA is "Receive a file" (Secondary rank, links to /download).
    - Trust badges render via `<Chip>` (neutral + accent), ALL-CAPS; the old `Badge` is gone.
    - Icons render via `@/components/icon`; zero lucide, zero emoji.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The hero is a flat app-landing with the "Send a file" primary CTA as focal point and Chip trust badges — no gradients.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the feature row with the Card recipe + Icon tiles; drop how-it-works/CTA-repeat</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (features section lines 107-140 using Card; how-it-works lines 142-167; CTA-repeat lines 169-189 — to drop/shorten)
    - 01-UI-SPEC.md "Card recipe (COMP-05)" (inner icon tile = --surface-subdued at --radius-md) + "Color" (accent is rare; primary buttons are ink)
    - 01-CONTEXT.md D-05 (tighter feature/trust row; drop how-it-works/CTA repetition)
    - src/components/ui/card.tsx, src/components/icon.tsx
  </read_first>
  <action>
    Keep a TIGHTER feature/trust row (do not restore the full 5-card marketing grid unless it stays compact) built from the shared `Card` primitives (recipe already applied in 01-03 — do NOT add a `border` utility or `bg-card/50` overlay; use the Card as-is). For each feature card: an inner icon tile styled `bg-[var(--surface-subdued)] rounded-[var(--radius-md)]` (12px) holding a single `<Icon>` (Untitled UI glyph via the wrapper — replace the lucide `Lock/Timer/Shield/Zap/Upload` icons), a `.gemba-h4` card title, and a `.gemba-body` subdued description. Do NOT use the accent blue as an icon-tile fill (accent is reserved for links/info/accent-chip; icon tiles use the subdued grey surface). DELETE the "How it works" section (lines 142-167) and the repeated bottom CTA section (lines 169-189) entirely per D-05 (streamline toward app landing). Remove now-unused imports (`Badge`, all `lucide-react` icons, and the `steps` array). Space major blocks with Gemba spacing (`--space-7`/`--space-8`). Use only Gemba tokens.
  </action>
  <verify>
    <automated>! grep -q "lucide-react" src/app/page.tsx && ! grep -qE "How it works|steps\s*=" src/app/page.tsx && ! grep -q "@/components/ui/badge" src/app/page.tsx && grep -qE "from\s+[\"']@/components/ui/card[\"']" src/app/page.tsx && grep -qE "radius-md|gemba-h4" src/app/page.tsx && npx tsc --noEmit 2>&1 | tail -3 && npm run build 2>&1 | tail -4 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Feature cards use the shared `Card` (no added `border`/overlay), each with a `--surface-subdued` `--radius-md` icon tile rendering an `<Icon>` glyph (no lucide).
    - Feature titles use `.gemba-h4`; descriptions use a subdued Gemba body class.
    - The "How it works" section, the repeated bottom CTA section, the `steps` array, the `Badge` import, and all `lucide-react` imports are removed.
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0 (no unused-import/type errors).
  </acceptance_criteria>
  <done>The home page is a tight app landing: hero + CTA focal point + compact feature row, all Gemba components, no marketing filler, builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The home page is static content + internal `<Link>`s to /upload and /download. It has no forms, no data fetches, no user input, and no network calls. The client-side-encryption boundary (crypto/upload/download/API) is entirely untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | (n/a) | static home page | accept | No trust boundary crossed — presentational page only; no input, storage, or network surface introduced. |
| T-01-06-SC | Tampering | package installs | n/a | No package-manager installs in this plan. |
</threat_model>

<verification>
- `npm run build` exits 0; `npm run lint` reports no new errors (no unused imports left behind).
- `grep` confirms page.tsx: no `gradient`/`bg-clip-text`/`text-transparent`, no `lucide-react`, no `Badge` import, no "How it works"; uses `@/components/chip`, `@/components/ui/card`, `@/components/icon`, `.gemba-h1`/`.gemba-h4`.
- Deferred to 01-07 (human): visual check of focal-point hierarchy and light/dark correctness.
</verification>

<success_criteria>
- PAGE-01: The home page is redesigned to the Gemba design system as an app landing — flat hero, "Send a file" focal CTA, Chip trust badges, Card-recipe feature row, all icons via the Icon wrapper, painted from tokens and correct in light and dark. No gradients, no emoji, no lucide.
</success_criteria>

<output>
Create `.planning/phases/01-design-foundation-home-page/01-06-SUMMARY.md` when done, listing the Untitled UI glyph names used and confirming the removed sections.
</output>
