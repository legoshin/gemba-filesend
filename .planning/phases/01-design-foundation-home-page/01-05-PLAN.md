---
phase: 01-design-foundation-home-page
plan: 5
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - src/components/app-shell.tsx
  - src/components/mobile-tab-bar.tsx
  - src/app/layout.tsx
autonomous: true
requirements: [PAGE-01, DARK-03]
must_haves:
  truths:
    - "On desktop (>= md) the app renders a fixed 240px white sidebar + top bar with content scrolling on --surface-page (D-03)"
    - "The active nav item gets a --surface-subdued fill and bold label via usePathname (D-03)"
    - "On mobile (< md) a fixed bottom tab bar (Home/Upload/Download) replaces the old Sheet drawer (D-04)"
    - "The Gemba wordmark swaps logo.svg <-> logo-dark.svg per theme; every shell icon renders via the Icon wrapper (DARK-03, D-07)"
  artifacts:
    - path: "src/components/app-shell.tsx"
      provides: "Desktop sidebar + top bar shell with active-nav state, brand lockup, theme toggle"
      exports: ["AppShell"]
      min_lines: 40
    - path: "src/components/mobile-tab-bar.tsx"
      provides: "Fixed bottom tab bar (3 tabs, Icon+label, aria-labels)"
      exports: ["MobileTabBar"]
      min_lines: 25
    - path: "src/app/layout.tsx"
      provides: "Root layout wired to the shell instead of Header"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/app-shell.tsx"
      via: "shell wraps <main>{children}</main>; Header removed"
      pattern: "AppShell"
    - from: "src/components/app-shell.tsx"
      to: "next/navigation usePathname"
      via: "active-route detection for nav fill + bold"
      pattern: "usePathname"
---

<objective>
Replace the sticky top-only `Header` with the Gemba app shell (D-03/D-04): a fixed 240px white sidebar + top bar on desktop, and a fixed bottom tab bar on mobile, with active-route highlighting, the theme-aware brand lockup (DARK-03), and every icon through the Icon wrapper (D-07). This is the layout chrome the home page (01-06) and all later pages render inside.

Purpose: Deliver the PAGE-01 layout paradigm (app shell) and DARK-03 (logo swaps per theme, mechanism preserved across web/PWA/TWA).
Output: New `app-shell.tsx` + `mobile-tab-bar.tsx`, and `layout.tsx` rewired to them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-design-foundation-home-page/01-UI-SPEC.md
@.planning/phases/01-design-foundation-home-page/01-PATTERNS.md
@.planning/phases/01-design-foundation-home-page/01-CONTEXT.md
@design-system/APPLY-GUIDE.md
@src/components/header.tsx
@src/app/layout.tsx
@src/components/theme-toggle.tsx
@src/components/icon.tsx
@src/components/ui/button.tsx
---
Note: `theme-toggle.tsx` (reskinned in 01-02) and `Button` (reskinned in 01-03) are consumed here but NOT modified — no file overlap. `layout.tsx` was touched by 01-01? No — 01-01 wired tokens via globals.css only, so layout.tsx is owned solely by this plan.
</context>

## Artifacts this phase produces (this plan)

- `src/components/app-shell.tsx` — `AppShell` client component: fixed 240px sidebar (`--surface-card`) with brand lockup + nav (active state via `usePathname`) + theme toggle, a top bar, and a scrolling content region on `--surface-page`; renders `MobileTabBar` for < md.
- `src/components/mobile-tab-bar.tsx` — `MobileTabBar` client component: fixed bottom tab bar (Home/Upload/Download), Icon + label per tab, `aria-label` on each, active state highlight.
- `src/app/layout.tsx` — root layout with `Header` removed and `AppShell` wrapping `children`.
- `src/components/header.tsx` — becomes unused after this plan (note it in the SUMMARY as dead code to delete once no page imports it; do NOT delete pre-existing files this phase unless nothing imports them).

<tasks>

<task type="auto">
  <name>Task 1: Build the desktop app shell (sidebar + top bar, active nav, brand lockup, logo swap)</name>
  <files>src/components/app-shell.tsx</files>
  <read_first>
    - src/components/header.tsx (nav data lines 11-15, logo-swap block lines 23-45, brand lockup — reuse verbatim mechanism)
    - 01-UI-SPEC.md "App shell & brand lockup" + "Color" (60/30/10) + Accessibility icon-button rule
    - design-system/APPLY-GUIDE.md Step 4 (240px sidebar, active item = --surface-subdued fill + bold)
    - 01-PATTERNS.md "header.tsx → split into app shell" (active-state via usePathname is NEW logic)
    - src/components/icon.tsx, src/components/theme-toggle.tsx, src/components/ui/button.tsx
  </read_first>
  <action>
    Create `src/components/app-shell.tsx` as a `"use client"` `AppShell({ children })` component. Reuse the `navLinks` array verbatim from header.tsx (`/`=Home, `/upload`=Upload, `/download`=Download). Layout: a fixed left sidebar `w-60` (240px) on `md:` and up, `bg-card` (Gemba white surface), containing at top the brand lockup — keep the EXACT dual-`<Image>` logo swap from header.tsx (`/logo.svg` with `dark:hidden`, `/logo-dark.svg` with `hidden dark:block`, same width/height/priority) plus the "Filesend" label restyled to `.gemba-body-sm` + `text-[var(--text-subdued)]` (was `text-muted-foreground`); do NOT rename the logo files (mechanism already correct, DARK-03). Below it, the nav: each link rendered with active detection via `usePathname()` from `next/navigation` — the item whose href matches the current path gets `bg-[var(--surface-subdued)]` fill + `.gemba-body-strong` (bold); inactive items are `.gemba-body` on transparent with a `hover:bg-[var(--surface-subdued)]`. Each nav item shows an Untitled UI `<Icon>` (via the wrapper) + label; confirm glyph names against icon-data.js (e.g. a home, upload/arrow-up, download/arrow-down glyph). Put the `<ThemeToggle />` in the sidebar footer or top bar. Add a slim top bar and a scrolling content region wrapping `{children}` on `bg-[var(--surface-page)]` with `md:pl-60` to clear the fixed sidebar. Render `<MobileTabBar />` (import from ./mobile-tab-bar) and hide the sidebar on mobile (`hidden md:flex`). Any icon-only control MUST carry an `aria-label` (UI-SPEC MANDATORY). Use only Gemba tokens.
  </action>
  <verify>
    <automated>grep -qE "usePathname" src/components/app-shell.tsx && grep -qE "w-60|w-\[240px\]" src/components/app-shell.tsx && grep -qE "dark:hidden" src/components/app-shell.tsx && grep -qE "hidden\s+.*dark:block|dark:block" src/components/app-shell.tsx && ! grep -q "lucide-react" src/components/app-shell.tsx && grep -qE "from\s+[\"']@/components/icon[\"']" src/components/app-shell.tsx && npx tsc --noEmit 2>&1 | tail -3 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `AppShell` renders a fixed 240px (`w-60`) `bg-card` sidebar on `md:`+, hidden on mobile; content region uses `bg-[var(--surface-page)]` and clears the sidebar (`md:pl-60`).
    - Active nav item (matched via `usePathname`) gets `--surface-subdued` fill + bold; inactive items are regular weight with subdued hover.
    - The dual-`<Image>` logo swap is preserved verbatim (`/logo.svg` dark:hidden, `/logo-dark.svg` dark:block); "Filesend" label uses `.gemba-body-sm` + `--text-subdued`.
    - Every shell icon renders via `@/components/icon`; zero `lucide-react`. Icon-only controls have `aria-label`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Desktop renders the Gemba sidebar+topbar shell with active-nav highlight and theme-correct branding.</done>
</task>

<task type="auto">
  <name>Task 2: Build the mobile bottom tab bar (D-04)</name>
  <files>src/components/mobile-tab-bar.tsx</files>
  <read_first>
    - src/components/header.tsx (mobile Sheet block lines 58-78 — the pattern being REPLACED, not reused)
    - 01-CONTEXT.md D-04 (fixed bottom tab bar for native-app feel, not a hamburger drawer)
    - 01-PATTERNS.md "Mobile nav pattern to replace, not reuse" + "No Analog Found" (build from navLinks + UI-SPEC only)
    - src/components/icon.tsx
  </read_first>
  <action>
    Create `src/components/mobile-tab-bar.tsx` as a `"use client"` `MobileTabBar()` component: a `<nav className="fixed inset-x-0 bottom-0 z-50 md:hidden ...">` on `bg-card` with a top inset-ring hairline (`shadow-[var(--ring-border)]`) and safe-area padding. Render the same three `navLinks` (Home/Upload/Download) as equal-width tab items, each a `<Link>` with an Untitled UI `<Icon>` (confirm glyph names against icon-data.js) stacked above a `.gemba-body-sm` label. Active tab (via `usePathname()`) gets `text-[var(--text-primary)]` + bold; inactive tabs get `text-[var(--text-subdued)]`. Each tab MUST have an `aria-label` matching its label (icon+label present, but ensure accessible name). Do NOT carry over the `Sheet`/`SheetTrigger`/`Menu` hamburger pattern. Use only Gemba tokens.
  </action>
  <verify>
    <automated>grep -qE "fixed\s+inset-x-0\s+bottom-0|bottom-0" src/components/mobile-tab-bar.tsx && grep -qE "md:hidden" src/components/mobile-tab-bar.tsx && ! grep -qE "Sheet|Menu" src/components/mobile-tab-bar.tsx && grep -qE "from\s+[\"']@/components/icon[\"']" src/components/mobile-tab-bar.tsx && grep -qE "usePathname" src/components/mobile-tab-bar.tsx && npx tsc --noEmit 2>&1 | tail -3 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `MobileTabBar` is a fixed bottom `<nav>` visible only `md:hidden`, on `bg-card` with a hairline top ring.
    - Three tabs (Home/Upload/Download) render Icon + label; active tab highlighted via `usePathname`.
    - No `Sheet`/`SheetTrigger`/`Menu` drawer pattern; each tab has an `aria-label`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Mobile shows a native-feel fixed bottom tab bar with three tabs, replacing the old drawer.</done>
</task>

<task type="auto">
  <name>Task 3: Wire layout.tsx to the shell (remove Header)</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (ThemeProvider wrapping `<div className="flex min-h-screen flex-col"><Header /><main>{children}</main></div>` — lines 41-55; metadata/viewport/Script blocks to preserve)
    - src/components/app-shell.tsx (from Task 1)
    - 01-PATTERNS.md "src/app/layout.tsx" section
  </read_first>
  <action>
    In `src/app/layout.tsx`, replace the `<Header />` usage: remove the `import { Header }` line and the `<div className="flex min-h-screen flex-col"><Header /><main className="flex-1">{children}</main></div>` structure, substituting `<AppShell>{children}</AppShell>` (import `AppShell` from `@/components/app-shell`). The `AppShell` owns the `<main>` region internally. Keep EVERYTHING else untouched: the `<ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>` wrapper (dark mode wiring), `<Toaster />`, the metadata/viewport exports, and the service-worker `<Script>` block. Do not modify `header.tsx` (it becomes unused; note for later deletion).
  </action>
  <verify>
    <automated>grep -qE "AppShell" src/app/layout.tsx && ! grep -qE "import\s+\{\s*Header\s*\}" src/app/layout.tsx && grep -qE "ThemeProvider" src/app/layout.tsx && grep -qE "sw-register" src/app/layout.tsx && npm run build 2>&1 | tail -4 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `layout.tsx` renders `<AppShell>{children}</AppShell>` inside the unchanged `ThemeProvider`; the `Header` import and usage are gone.
    - `ThemeProvider` props, `<Toaster />`, metadata/viewport, and the SW `<Script>` are unchanged.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>The whole app renders inside the Gemba shell; the old Header is no longer mounted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational layout chrome + client-side routing only. Logo assets are static files already in `public/`. No network, data-handling, or user-input surface is added; the client-side-encryption boundary is untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Spoofing (brand) | theme-keyed logo swap | accept | Uses the existing verified dual-`<Image>` mechanism keyed off `next-themes` `.dark` class; static in-repo assets, no dynamic src. Platform parity (web/PWA/TWA) preserved because filenames/mechanism are unchanged. |
| T-01-05-SC | Tampering | package installs | n/a | No package-manager installs in this plan. |
</threat_model>

<verification>
- `npm run build` exits 0; `npm run lint` reports no new errors.
- `grep` confirms: `usePathname` in both nav components, no `lucide-react` in shell files, logo `dark:hidden`/`dark:block` preserved, `Header` import removed from layout.
- Deferred to 01-07 (human): visual check that the sidebar/top bar/tab bar render correctly and the logo swaps in light/dark.
</verification>

<success_criteria>
- PAGE-01 (layout): the Gemba 240px sidebar + top bar (desktop) and bottom tab bar (mobile) replace the old header, with active-route highlighting.
- DARK-03: the wordmark swaps to the correct asset per theme via the preserved mechanism; shell icons render via the Icon wrapper.
</success_criteria>

<output>
Create `.planning/phases/01-design-foundation-home-page/01-05-SUMMARY.md` when done, listing the Untitled UI glyph names used for the nav/tab icons and noting `header.tsx` as now-unused.
</output>
