---
phase: 01-design-foundation-home-page
plan: 4
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/ui/input.tsx
  - src/components/ui/switch.tsx
  - src/components/ui/checkbox.tsx
  - src/components/ui/radio-group.tsx
  - components.json
  - package.json
  - package-lock.json
autonomous: false
requirements: [COMP-02]
user_setup: []
must_haves:
  truths:
    - "Input renders the Gemba field recipe — 40px tall, 8px radius, inset hairline ring + soft field shadow, ink focus ring (COMP-02)"
    - "Switch/Toggle renders the Gemba pill track (ink when on) via tokens (COMP-02)"
    - "Checkbox and RadioGroup exist in the component layer and match the Gemba forms spec — filled ink when selected (COMP-02)"
    - "New form primitives were installed from the shadcn OFFICIAL registry only, verified before install"
  artifacts:
    - path: "src/components/ui/checkbox.tsx"
      provides: "Radix-backed Checkbox reskinned to Gemba tokens"
      exports: ["Checkbox"]
    - path: "src/components/ui/radio-group.tsx"
      provides: "Radix-backed RadioGroup reskinned to Gemba tokens"
      exports: ["RadioGroup", "RadioGroupItem"]
    - path: "src/components/ui/input.tsx"
      provides: "Gemba field recipe"
      exports: ["Input"]
    - path: "src/components/ui/switch.tsx"
      provides: "Gemba toggle track"
      exports: ["Switch"]
  key_links:
    - from: "src/components/ui/input.tsx"
      to: "--ring-border / --ring-focus / --shadow-field"
      via: "field recipe box-shadow + focus ring"
      pattern: "--ring-focus"
---

<objective>
Complete the shared form-control layer to the Gemba spec (COMP-02): reskin the existing `Input` and `Switch` to Gemba field/toggle tokens, and add `Checkbox` + `RadioGroup` (which do not yet exist) from the shadcn OFFICIAL registry, then reskin them. The home page has no forms, but COMP-02 is a Phase 1 requirement — these primitives must exist and match the design system so Phases 2–3 (upload/download) reuse them without re-litigation.

Purpose: Satisfy COMP-02 (Input, Checkbox, Radio, Toggle match the design system).
Output: Reskinned `input.tsx`/`switch.tsx`, new `checkbox.tsx`/`radio-group.tsx`.

This plan is NON-autonomous: it installs npm packages via `npx shadcn add`, gated by a blocking supply-chain checkpoint (the one real risk class this phase).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-design-foundation-home-page/01-UI-SPEC.md
@design-system/components/forms/Input.jsx
@design-system/components/forms/Toggle.jsx
@design-system/components/forms/Checkbox.jsx
@design-system/components/forms/Radio.jsx
@design-system/tokens/spacing.css
@src/components/ui/input.tsx
@src/components/ui/switch.tsx
@components.json
</context>

## Artifacts this phase produces (this plan)

- `src/components/ui/input.tsx` — Gemba field recipe (reskin).
- `src/components/ui/switch.tsx` — Gemba toggle track (reskin).
- `src/components/ui/checkbox.tsx` — NEW `Checkbox` (Radix), reskinned to Gemba tokens.
- `src/components/ui/radio-group.tsx` — NEW `RadioGroup` + `RadioGroupItem` (Radix), reskinned to Gemba tokens.
- Added npm deps (from `npx shadcn add checkbox radio-group`): `@radix-ui/react-checkbox`, `@radix-ui/react-radio-group` (or resolved via the existing `radix-ui` meta-package) — recorded in package.json / package-lock.json.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Supply-chain gate — verify checkbox + radio-group are shadcn OFFICIAL registry</name>
  <what-built>
    Nothing yet — this checkpoint gates the `npx shadcn add checkbox radio-group` install in Task 2. No RESEARCH.md Package Legitimacy Audit exists for this phase, so per the planner package-legitimacy fallback the added packages are treated as [ASSUMED] and require human verification before install.
  </what-built>
  <how-to-verify>
    1. Confirm `components.json` has `"registries": {}` (no third-party registries configured) — run: `node -e "console.log(require('./components.json').registries || 'none')"`.
    2. Confirm the two components come from the shadcn OFFICIAL registry (not a third-party URL): the install command is `npx shadcn@latest add checkbox radio-group` with NO `@<registry>` prefix and NO custom registry URL.
    3. Verify each component/dep on its official page:
       - shadcn checkbox: https://ui.shadcn.com/docs/components/checkbox
       - shadcn radio-group: https://ui.shadcn.com/docs/components/radio-group
       - npm: https://www.npmjs.com/package/@radix-ui/react-checkbox and https://www.npmjs.com/package/@radix-ui/react-radio-group (published by the Radix team; the app already depends on `radix-ui`).
    4. Confirm the app already uses Radix primitives (`radix-ui` in package.json, e.g. switch.tsx imports from `radix-ui`) — so these add no new vendor, only official Radix packages.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or describe concerns (e.g. an unexpected registry/URL) to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install checkbox + radio-group from the official registry and reskin all four controls</name>
  <files>src/components/ui/checkbox.tsx, src/components/ui/radio-group.tsx, src/components/ui/input.tsx, src/components/ui/switch.tsx, components.json, package.json, package-lock.json</files>
  <read_first>
    - src/components/ui/input.tsx (current field classes to reskin) and src/components/ui/switch.tsx (current track/thumb)
    - design-system/components/forms/Input.jsx (40px tall, 8px radius, white surface, inset 1px hairline + soft shadow, focus → 2px ink ring; error/success rings)
    - design-system/components/forms/Toggle.jsx (36×20 track, 16px knob, ink track when on)
    - design-system/components/forms/Checkbox.jsx (18×18 box, 6px radius, ink fill + white check when on, 1.5px #9499A4 ring when off)
    - design-system/components/forms/Radio.jsx (18×18 circle, ink ring + ink dot when selected)
    - design-system/tokens/spacing.css (`--ring-border`, `--ring-focus`, `--shadow-field`, `--radius-sm`)
  </read_first>
  <action>
    First run `npx shadcn@latest add checkbox radio-group` (official registry, as approved in Task 1) to scaffold `src/components/ui/checkbox.tsx` and `src/components/ui/radio-group.tsx`. Then reskin ALL FOUR controls to Gemba tokens (only tokens from the design system — no invented values):
    - Input (`input.tsx`): height 40px (`h-10`), `rounded-[var(--radius-sm)]` (8px), `bg-[var(--surface-card)]`, hairline via `shadow-[var(--ring-border),var(--shadow-field)]` (drop the Tailwind `border` class), padding `px-4` (12–16px per spec). Focus: swap the shadcn `ring`/`border-ring` focus for the ink ring `focus-visible:shadow-[var(--ring-focus)]` (2px ink). Keep placeholder/disabled/aria-invalid handling; error state maps to `--gemba-critical`.
    - Switch (`switch.tsx`): keep the Radix `Root`/`Thumb` structure; track `data-[state=checked]:bg-[var(--button-primary-bg)]` (ink when on) and `data-[state=unchecked]:bg-[var(--gemba-ink-400)]`, `rounded-[var(--radius-pill)]`; white knob with the soft knob shadow from Toggle.jsx.
    - Checkbox (`checkbox.tsx`): 18×18, `rounded-[6px]`, unchecked = `shadow-[inset_0_0_0_1.5px_var(--gemba-ink-400)]` on `--surface-card`; checked = `bg-[var(--button-primary-bg)]` (ink) with a white check; focus ring = `--ring-focus`.
    - RadioGroup (`radio-group.tsx`): item 18×18 circle, ink ring (`inset 0 0 0 1.5px var(--gemba-ink-800)` selected / `--gemba-ink-400` idle) on `--surface-card`, ink dot when selected; focus ring = `--ring-focus`.
    Keep all Radix a11y (roles, `data-state`, keyboard) intact. None of these are used by the home page — do NOT add them to page.tsx.
  </action>
  <verify>
    <automated>test -f src/components/ui/checkbox.tsx && test -f src/components/ui/radio-group.tsx && grep -qE "var\(--ring-focus\)" src/components/ui/input.tsx && grep -qE "var\(--button-primary-bg\)" src/components/ui/switch.tsx && grep -qE "var\(--ring-focus\)" src/components/ui/checkbox.tsx && npx tsc --noEmit 2>&1 | tail -3 && npm run build 2>&1 | tail -3 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `checkbox.tsx` and `radio-group.tsx` exist (scaffolded from the official registry) and export `Checkbox` / `RadioGroup`+`RadioGroupItem`.
    - Input = 40px tall, 8px radius, `shadow-[var(--ring-border),var(--shadow-field)]`, ink focus ring (`--ring-focus`), no Tailwind `border` class.
    - Switch track = ink when on (`--button-primary-bg`), pill radius; Checkbox = ink fill + white check when on, 1.5px idle ring; Radio = ink ring + ink dot when selected.
    - package.json records only official Radix deps (`@radix-ui/react-checkbox`, `@radix-ui/react-radio-group`, or resolved via `radix-ui`); `components.json` registries remains `{}`.
    - `npx tsc --noEmit` and `npm run build` exit 0.
  </acceptance_criteria>
  <done>Input, Switch, Checkbox, and RadioGroup all exist and render the Gemba forms recipe from tokens; new deps came only from the official registry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → npm registry / shadcn registry | `npx shadcn add checkbox radio-group` fetches component source and installs npm dependencies. This is the ONE real threat surface this phase introduces. No runtime data-handling or network surface is added to the app; the client-side-encryption boundary is untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-SC | Tampering (supply chain) | `npx shadcn add checkbox radio-group` + added `@radix-ui/*` deps | mitigate | Blocking human checkpoint (Task 1) verifies the components come from the shadcn OFFICIAL registry only (`components.json` registries `{}`, no `@registry` prefix / custom URL) and that the added deps are official Radix packages already in the app's trust set. Per UI-SPEC Registry Safety: shadcn official registry only; third-party = none. Checkpoint is non-auto-approvable (auto_advance ignored). |
| T-01-04-01 | Tampering | Radix form primitives at runtime | accept | Radix is already a first-class dependency (`radix-ui`); reskinning is class-string only, no logic change, no new data flow. |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` exit 0.
- `node -e "console.log(require('./components.json').registries||'none')"` prints `none`/`{}`.
- `npm run lint` reports no new errors.
- Input/Switch/Checkbox/Radio all reference Gemba tokens (`--ring-border`, `--ring-focus`, `--button-primary-bg`).
</verification>

<success_criteria>
- COMP-02: Input, Checkbox, Radio, and Toggle (Switch) exist in the component layer and match the Gemba forms spec, painted from tokens, with new primitives sourced only from the shadcn official registry.
</success_criteria>

<output>
Create `.planning/phases/01-design-foundation-home-page/01-04-SUMMARY.md` when done, recording the exact packages/versions added and confirming official-registry provenance.
</output>
