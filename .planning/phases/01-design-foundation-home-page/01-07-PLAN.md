---
phase: 01-design-foundation-home-page
plan: 7
type: execute
wave: 4
depends_on: [01-05, 01-06]
files_modified: []
autonomous: false
requirements: [PAGE-01, DARK-01, DARK-03, DESIGN-02]
must_haves:
  truths:
    - "A human has confirmed the home page + shell render correctly in BOTH light and dark via the theme toggle"
    - "The Gemba wordmark shows the correct asset in each theme; no unstyled or mis-coloured element appears in dark mode"
    - "All UI icons render as Untitled UI stroke glyphs (Icon wrapper) — no emoji, no lucide on the home page or shell"
    - "No gradient hero / radial background remains; the 'Send a file' primary CTA is the clear focal point"
  artifacts: []
  key_links: []
---

<objective>
Phase-level human verification gate. All implementation plans (01-01 tokens/dark, 01-02 icons, 01-03 components, 01-04 forms, 01-05 shell, 01-06 home page) are autonomous; this plan isolates the single blocking human-verify checkpoint that confirms the phase's visual success criteria across their combined output — token fidelity, light/dark correctness, logo swap, icon system, and the app-landing focal point.

Purpose: Verify ROADMAP Phase 1 Success Criteria 1–5 (Gemba tokens on the home page, light/dark + correct logo, shared component layer in use, Icon wrapper for all icons, every alias resolves in dark).
Output: Human sign-off (or a punch list of visual defects feeding a gap-closure pass).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-design-foundation-home-page/01-UI-SPEC.md
@.planning/ROADMAP.md
</context>

## Artifacts this phase produces (this plan)

- No source artifacts. Produces a verification record (SUMMARY) capturing the human sign-off or defect list.

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight automated gates before human review</name>
  <files></files>
  <read_first>
    - src/app/globals.css, src/app/page.tsx, src/components/app-shell.tsx, src/components/mobile-tab-bar.tsx (final state from prior plans)
  </read_first>
  <action>
    Run the automated phase gates so the human only reviews a building app: confirm `npm run build` and `npm run lint` pass, and confirm the token/guardrail invariants via grep — no `oklch(` in globals.css; no `lucide-react` and no gradient utilities in `src/app/page.tsx`, `src/components/app-shell.tsx`, or `src/components/mobile-tab-bar.tsx`. Start the dev server (`npm run dev`) so the reviewer can open the app. Report any gate failure instead of proceeding to the checkpoint.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -4 && npm run lint 2>&1 | tail -4 && ! grep -q "oklch(" src/app/globals.css && ! grep -RqE "lucide-react|bg-gradient|radial-gradient|bg-clip-text" src/app/page.tsx src/components/app-shell.tsx src/components/mobile-tab-bar.tsx && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` and `npm run lint` exit 0.
    - No `oklch(` in globals.css; no lucide/gradient utilities in page.tsx, app-shell.tsx, mobile-tab-bar.tsx.
    - Dev server is running and reachable for the reviewer.
  </acceptance_criteria>
  <done>All automated phase gates pass; the app is running for human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification — light/dark, logo, icons, focal point</name>
  <what-built>
    The full Phase 1 output: Gemba tokens wired app-wide with a near-black dark layer (01-01), the Icon wrapper (01-02), reskinned Button/Card/Chip (01-03), the form-control layer (01-04), the sidebar/top-bar + mobile tab-bar shell (01-05), and the redesigned home page (01-06) — all running on the dev server.
  </what-built>
  <how-to-verify>
    1. Open the app (e.g. http://localhost:3000) in a desktop-width window.
    2. LIGHT MODE — confirm on the home page: the "Send a file" ink pill is the clear dominant focal point; the headline is flat ink `.gemba-h1` (NO gradient text); the background is flat (NO radial gradient); trust badges are ALL-CAPS Chips (OPEN SOURCE neutral, END-TO-END ENCRYPTED accent-blue); feature cards are white with a soft hairline ring + shadow (no hard border); all icons are thin Untitled UI stroke glyphs (no emoji). The 240px white sidebar shows the Gemba wordmark + "Filesend", nav with the active item filled + bold.
    3. Toggle to DARK MODE via the theme toggle. Confirm: page + sidebar go true near-black / step-lighter card; text stays legible (no invisible or mis-coloured elements); accent-blue link/chip and any signal colours read clearly (lightened, not muddy); the wordmark SWAPS to the dark logo asset; nothing appears unstyled.
    4. Resize to mobile width (< md): the sidebar is replaced by a fixed bottom tab bar (Home/Upload/Download) with icon+label; the active tab is highlighted; there is NO hamburger drawer.
    5. Tab/keyboard: the theme toggle and any icon-only controls announce an accessible name (aria-label).
    6. Confirm no emoji is used as a UI icon anywhere on the home page or shell.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or list the specific visual defects (element + light/dark + expected vs actual) to drive a gap-closure pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Verification-only plan. No code changes, no installs, no new surface. The client-side-encryption boundary is untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-SC | Tampering | package installs | n/a | No package-manager installs; this plan runs build/lint/dev and a human review only. |
</threat_model>

<verification>
- Automated gates (Task 1) pass.
- Human reviewer types "approved" (Task 2), or a defect list is recorded for gap closure.
</verification>

<success_criteria>
- ROADMAP Phase 1 Success Criteria 1–5 confirmed by a human: Gemba tokens on the home page (no raw values), correct light/dark rendering incl. the theme-correct logo, shared component layer in use, all icons via the Icon wrapper (no emoji), and every semantic alias resolving to a valid dark value.
</success_criteria>

<output>
Create `.planning/phases/01-design-foundation-home-page/01-07-SUMMARY.md` when done, recording the sign-off (or defect list) and the phase-level verification result.
</output>
