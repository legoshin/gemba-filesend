---
phase: 11-page-motion
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/app/download/page.tsx
autonomous: true
requirements: [MOT-01, MOT-02]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "The download page animates its primary content (Enter Share Link / File Details / file-row states) in on mount with the shared PageEntrance stagger."
    - "A thin scroll-progress bar tracks scroll position on the download page (which is long)."
    - "Entrance + scroll-progress degrade correctly under prefers-reduced-motion (inherited from the shared components + app-root MotionConfig)."
    - "No crypto/storage/multi-file logic, decrypt flow, live download progress UI, or colour/type token is modified."
  artifacts:
    - src/app/download/page.tsx
  key_links:
    - "ScrollProgress is mounted ONCE in the download page's primary (long) content return as a fixed overlay; it does not wrap or alter the decrypt/download logic."
    - "PageEntrance/PageEntranceItem wrap the presentation cards only — decryptPacked/importKeyBase64, the existing AnimatePresence file rows, and the Skeleton loading state keep their exact behavior."
---

<objective>
Apply the Wave-1 tracer components to the download page: wrap its primary content in the shared `PageEntrance` stagger (MOT-01, download) and mount the shared `ScrollProgress` bar (MOT-02, download — the download page is long and scrolls).

Purpose: Deliver the proven entrance + scroll-progress pattern on the second long page. This is a surgical, presentation-only wrapping — the client-side decryption path, the download flow, the existing per-file `AnimatePresence` rows, and the Skeleton loading state are untouched.
Output: `src/app/download/page.tsx` renders staggered entrance on its cards and a scroll-progress bar, with `npm run build` + `npm test` green.

Consumes (from 11-01): `PageEntrance`/`PageEntranceItem` from "@/components/page-entrance"; `ScrollProgress` from "@/components/scroll-progress".
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-page-motion/11-CONTEXT.md
@.planning/phases/11-page-motion/11-01-SUMMARY.md
@src/components/page-entrance.tsx
@src/components/scroll-progress.tsx
@src/app/download/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap download primary content in PageEntrance + mount ScrollProgress (surgical)</name>
  <files>src/app/download/page.tsx</files>
  <read_first>
    - src/app/download/page.tsx — the `DownloadPage` component (from line ~171): the primary content `return` (root `<div className="mx-auto max-w-2xl px-4 py-12 sm:px-6 sm:py-16">`, ~line 604) and the per-state `<Card>` blocks (Fetching File Info, Enter Share Link, File Details, and the error/empty cards). Identify the top-level presentation cards that should stagger in per state.
    - src/components/page-entrance.tsx — `PageEntrance({children,className?})` (container) and `PageEntranceItem({children,className?})` (item; inherits parent labels).
    - src/components/scroll-progress.tsx — `ScrollProgress()` (no props, fixed overlay).
    - 11-01-SUMMARY.md — exact component signatures + staggerContainer value.
    - Note the existing `AnimatePresence` file-row motion (`DownloadFileRow`) and `Skeleton` loading state — leave these exactly as-is; the page entrance wraps the CARD level, not the row level.
  </read_first>
  <action>
    In `DownloadPage`'s primary content return, replace the root `<div className="mx-auto max-w-2xl px-4 py-12 sm:px-6 sm:py-16">` with `<PageEntrance className="mx-auto max-w-2xl px-4 py-12 sm:px-6 sm:py-16">`, and wrap each top-level presentation `<Card>` for the page's states (Enter Share Link, File Details, and the error/empty cards) in a `<PageEntranceItem>` so the primary content animates in on mount. If distinct states render via separate root containers, wrap each state's top-level card the same way. Preserve every existing className, prop, handler, state branch, and child byte-for-byte — you are only inserting wrapper elements. Do NOT wrap or alter the inner `AnimatePresence`/`DownloadFileRow` rows or the `Skeleton` loading card's internals — page entrance operates at the card level only.

    Mount `<ScrollProgress />` ONCE as the first child inside the primary content return's top-level container (it is `fixed`, so DOM position does not affect layout). Import `ScrollProgress` from "@/components/scroll-progress" and `PageEntrance`, `PageEntranceItem` from "@/components/page-entrance".

    Do NOT modify: `decryptPacked`/`importKeyBase64` or any crypto/storage/multi-file logic, the download/fetch flow, the live `<Progress>` download-progress UI behavior, the existing file-row `AnimatePresence`, the `Skeleton` loading behavior, or any colour/type token. This is a presentation-wrapper change only. The page is already `"use client"`, so no boundary change is needed. Do not use `motion.create(...)` here — wrapping is entirely via the shared components.
  </action>
  <verify>
    <automated>grep -q 'PageEntrance' src/app/download/page.tsx && grep -q 'PageEntranceItem' src/app/download/page.tsx && grep -q 'ScrollProgress' src/app/download/page.tsx && grep -q 'from "@/components/page-entrance"' src/app/download/page.tsx && grep -q 'from "@/components/scroll-progress"' src/app/download/page.tsx && grep -q 'AnimatePresence' src/app/download/page.tsx && grep -q 'Skeleton' src/app/download/page.tsx && npm run build && npm test && test -z "$(git status --porcelain -- src/lib/crypto.ts src/lib/storage.ts src/lib/server-storage.ts src/lib/blob-storage.ts src/lib/multi-file.ts design-system/tokens/)"</automated>
    <fails_when>download/page.tsx does not import or use PageEntrance/PageEntranceItem/ScrollProgress; the existing AnimatePresence rows or Skeleton were removed; `npm run build` fails; `npm test` regresses below the green baseline; or ANY protected file (crypto.ts, storage.ts, server-storage.ts, blob-storage.ts, multi-file.ts, design-system/tokens/) shows a git diff.</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/app/download/page.tsx` wraps its primary content cards in `PageEntrance` + `PageEntranceItem` from the shared component.
    - `<ScrollProgress />` is mounted exactly once in the primary content return.
    - The existing `AnimatePresence` file rows and `Skeleton` loading state are preserved unchanged; all decrypt/download/live-progress logic and every className/prop is byte-for-byte unchanged apart from the inserted wrapper elements.
    - `npm run build` and `npm test` are green; no diff to any protected crypto/storage/multi-file/token path.
  </acceptance_criteria>
  <done>The download page staggers its primary content in on load and shows a scroll-progress bar as the user scrolls; reduced motion is honored; the decryption boundary, download flow, existing row motion, and tokens are untouched.</done>
  <reversibility rating="reversible">Inserts shared wrapper elements + one component mount into one file — revert by unwrapping.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ download page render | Entrance/scroll motion is client-side presentation; reads no untrusted input. |
| motion wrappers ↔ decrypt/download flow | Wrappers must not reach into `decryptPacked`/`importKeyBase64` or the client-side-encryption path (key comes from the URL fragment, never the server). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07 | Tampering | src/lib/crypto.ts, storage.ts, multi-file.ts (via download/page.tsx) | high | mitigate | `<verify>` git-diff-guard asserts zero diff to crypto/storage/multi-file; the change only inserts presentation wrappers around existing cards. |
| T-11-08 | Tampering | design-system/tokens/ (colour/type) | medium | mitigate | git-diff-guard asserts zero token diff; scroll bar/entrance reuse existing tokens/presets. |
| T-11-09 | Denial of Service | download page scroll handler (ScrollProgress) | low | accept | Passive MotionValue-driven bar, no per-scroll state churn; reduced-motion drops the spring. |
</threat_model>

<verification>
- `npm run build` green.
- `npm test` full suite green, no regression (baseline ≥127).
- `test "$(npx tsc --noEmit 2>&1 | grep 'error TS' | grep -vc 'crypto.test.ts')" = "0"`.
- git-diff-guard: no diff to crypto/storage/server-storage/blob-storage/multi-file or design-system/tokens.
- Manual (Phase 12, not a gate): download cards stagger in; scroll bar tracks; existing file-row motion, Skeleton, decrypt, and live download progress unchanged in light/dark/system.
</verification>

<success_criteria>
- Download page animates its primary content in on load (MOT-01, download).
- A scroll-progress bar tracks scroll on the download page (MOT-02, download).
- Decrypt/download flow, existing row motion, live progress UI, encryption boundary, and colour/type tokens are unchanged.
</success_criteria>

<output>
Create `.planning/phases/11-page-motion/11-03-SUMMARY.md` when done.
</output>
