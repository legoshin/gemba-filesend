---
phase: 01-design-foundation-home-page
plan: 3
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/chip.tsx
autonomous: true
requirements: [COMP-01, COMP-03, COMP-05]
must_haves:
  truths:
    - "Button exposes Gemba ranks (Primary ink pill / Secondary / Tertiary / Ghost) in default/small/square shapes (COMP-01)"
    - "Card renders the Gemba recipe — white surface, 16px radius, inset-ring border + soft cool-grey shadow, NO Tailwind border (COMP-05)"
    - "A Chip component renders ALL-CAPS 10px signal labels on an 8% tint with an optional Icon-wrapper prefix (COMP-03)"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "Gemba button ranks + sizes via retrofitted cva"
      exports: ["Button", "buttonVariants"]
    - path: "src/components/ui/card.tsx"
      provides: "Card recipe (inset ring + soft shadow, no border)"
      exports: ["Card"]
    - path: "src/components/chip.tsx"
      provides: "Chip/status component (neutral/accent/success/warning/critical)"
      exports: ["Chip", "chipVariants"]
      min_lines: 30
  key_links:
    - from: "src/components/ui/card.tsx"
      to: "--ring-border + --shadow-card"
      via: "box-shadow recipe replacing the Tailwind border class"
      pattern: "shadow-\\[var\\(--ring-border\\)"
    - from: "src/components/chip.tsx"
      to: "src/components/icon.tsx"
      via: "optional prefix icon slot"
      pattern: "from\\s+[\"']@/components/icon[\"']"
---

<objective>
Build the core reusable component layer on top of the re-pointed tokens (01-01) and the Icon wrapper (01-02): retrofit `Button` to the Gemba button ranks (COMP-01), retrofit `Card` to the inset-ring card recipe (COMP-05), and add a new `Chip` (COMP-03). These are the shared components the home page (01-06) and shell (01-05) consume.

Purpose: Satisfy COMP-01 (button ranks), COMP-05 (card recipe), COMP-03 (chip). Resolves the CONTEXT.md "Claude's Discretion" items in favour of retrofitting existing shadcn primitives (reuse-first).
Output: Reskinned `button.tsx`, `card.tsx`, and a new `chip.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-design-foundation-home-page/01-UI-SPEC.md
@.planning/phases/01-design-foundation-home-page/01-PATTERNS.md
@design-system/APPLY-GUIDE.md
@design-system/tokens/spacing.css
@design-system/components/core/Chip.jsx
@src/components/ui/button.tsx
@src/components/ui/card.tsx
@src/components/ui/badge.tsx
</context>

## Artifacts this phase produces (this plan)

- `src/components/ui/button.tsx` — retrofitted `cva` `variant` keys mapped to Gemba ranks: `default` (Primary ink pill), `secondary`, `tertiary` (NEW key), `ghost`; sizes `default` (40px), `sm` (32px), plus square icon variants at `--radius-sm`. `data-slot`/`data-variant`/`data-size` attributes preserved.
- `src/components/ui/card.tsx` — `Card` base restyled to the COMP-05 recipe (no `border` class).
- `src/components/chip.tsx` — NEW `Chip` + `chipVariants`, `cva`-based, following `badge.tsx` conventions, with an optional Icon-wrapper prefix slot.

<tasks>

<task type="auto">
  <name>Task 1: Retrofit Button to Gemba ranks + sizes (COMP-01)</name>
  <files>src/components/ui/button.tsx</files>
  <read_first>
    - src/components/ui/button.tsx (existing cva variant/size API — retrofit in place)
    - 01-UI-SPEC.md "Button ranks" table + "Accessibility (icon-only / square variants)" note
    - design-system/APPLY-GUIDE.md Step 3 Buttons; design-system/components/core/PrimaryButton.jsx / SecondaryButton.jsx / TertiaryButton.jsx / GhostButton.jsx
    - 01-PATTERNS.md "src/components/ui/button.tsx" section
  </read_first>
  <action>
    Retrofit the `cva` `variant` map to the four Gemba ranks (per UI-SPEC Button ranks table): `default` = Primary — `bg-primary text-primary-foreground` (which now resolve to ink `--button-primary-bg` / white via 01-01) with a subtle hover; `secondary` = `bg-secondary text-secondary-foreground` (Gemba `--button-emphasized-bg` #E8EAED); add a NEW `tertiary` variant = `bg-[var(--button-subdued-bg)] text-primary` (#F3F5F6); `ghost` = transparent with `hover:bg-[var(--surface-subdued)]`. Keep the `destructive`, `outline`, `link` variants intact for later phases but do not use them on home. Retrofit shapes: pill default/small use `--radius-xl` (24px) → `rounded-[var(--radius-xl)]`; square (`icon`, `icon-sm`) variants use `--radius-sm` (8px) → `rounded-[var(--radius-sm)]`. Sizes: `default` = 40px tall (`h-10`), padding `px-6` (24px) `gap-2` (8px), 20px icons; `sm` = 32px tall (`h-8`), padding `px-4` (16px), 16px icons; `icon`/`icon-sm` = square 40px/32px. Keep `data-slot`/`data-variant`/`data-size` attributes and the `Slot`/`asChild` pattern. Keep focus visible via the ink focus ring intent (use `--ring-focus` where the current ring-based focus is applied). Labels are verb-first (enforced at call sites, not here). Do NOT hardcode hex — reach through the Gemba-backed utilities / `var(--...)` tokens only.
  </action>
  <verify>
    <automated>grep -qE "tertiary:" src/components/ui/button.tsx && grep -qE "rounded-\[var\(--radius-xl\)\]" src/components/ui/button.tsx && grep -qE "h-10" src/components/ui/button.tsx && npx tsc --noEmit 2>&1 | tail -3 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `variant` map has `default` (Primary ink pill), `secondary`, `tertiary` (new), `ghost` mapped to Gemba token-backed utilities; no raw hex added.
    - Pill ranks use `--radius-xl` (24px); square/icon variants use `--radius-sm` (8px).
    - `default` size = 40px tall / 20px icons; `sm` = 32px tall / 16px icons.
    - `data-slot`/`data-variant`/`data-size` and `asChild` behaviour preserved; `buttonVariants` still exported.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Button offers all four Gemba ranks in default/small/square shapes, painted from tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Retrofit Card to the inset-ring recipe (COMP-05)</name>
  <files>src/components/ui/card.tsx</files>
  <read_first>
    - src/components/ui/card.tsx (line 10 base class with `border` + `shadow-sm` + `rounded-xl` — the guardrail violation to fix)
    - 01-UI-SPEC.md "Borders & Shadows" + "Card recipe (COMP-05)"; design-system/tokens/spacing.css (`--ring-border`, `--shadow-card`, `--radius-lg`)
    - 01-PATTERNS.md "src/components/ui/card.tsx" section (explicit "do NOT use Tailwind border" guardrail)
  </read_first>
  <action>
    Change the `Card` base class (currently `bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 shadow-sm`): REMOVE the `border` utility class entirely and REPLACE `shadow-sm` with the Gemba card recipe realised as an arbitrary box-shadow that stacks the inset ring and the soft card shadow — `shadow-[var(--ring-border),var(--shadow-card)]`; change `rounded-xl` → `rounded-lg` (16px = `--radius-lg` per COMP-05). Keep `bg-card` (now Gemba white surface via 01-01), `text-card-foreground`, the flex/gap/padding layout, and all other Card sub-components (`CardHeader`/`CardContent`/etc.) unchanged. This is the one shadcn primitive with an explicit "borders are inset rings, never CSS `border`" guardrail — no `border` class may remain on the Card surface.
  </action>
  <verify>
    <automated>grep -qE "shadow-\[var\(--ring-border\),var\(--shadow-card\)\]" src/components/ui/card.tsx && grep -qE "rounded-lg" src/components/ui/card.tsx && ! grep -nE "\"[^\"]*\\bborder\\b[^\"]*\"" src/components/ui/card.tsx | grep -q "data-slot=\"card\"" ; npx tsc --noEmit 2>&1 | tail -3 && echo done</automated>
  </verify>
  <acceptance_criteria>
    - The `Card` base class no longer contains the `border` utility; it uses `shadow-[var(--ring-border),var(--shadow-card)]`.
    - Corner radius is `rounded-lg` (16px).
    - `bg-card` retained (Gemba white surface); Card sub-components unchanged and still exported.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Cards render as white 16px-radius surfaces with an inset hairline ring + soft cool-grey shadow and no CSS border.</done>
</task>

<task type="auto">
  <name>Task 3: Create Chip component (COMP-03)</name>
  <files>src/components/chip.tsx</files>
  <read_first>
    - design-system/components/core/Chip.jsx (reference — exact dims: height 20, border-radius 16, padding 3px 10px, gap 4, label 10px/16px weight 700 ALL-CAPS, bg = signal 8% tint, text = full signal)
    - src/components/ui/badge.tsx (cva + Comp/asChild convention to mirror)
    - src/components/icon.tsx (optional prefix icon slot)
    - 01-UI-SPEC.md "Chip (COMP-03)"; design-system/tokens/colors.css (`--gemba-{signal}` + `--gemba-{signal}-subdued`)
  </read_first>
  <action>
    Create `src/components/chip.tsx` as a `cva`-based component following the `badge.tsx` structure (variant map + `Comp`/`asChild` via `Slot`, `data-slot="chip"`), but with the Gemba Chip spec instead of Badge's pill-outline. Base layout: `inline-flex items-center` with `gap-[var(--space-2)]` (4px), `h-5` (20px), `rounded-[var(--radius-lg)]` (16px — matches the reference's borderRadius 16), padding `px-2.5 py-[3px]` (10px/3px), `w-fit`. Label typography via the `.gemba-chip-label` helper class (10px/16px, weight 700) plus `uppercase`. Variant map (background = 8% subdued tint, text = full-strength signal), all from colors.css semantic aliases: `neutral` → `bg-[var(--gemba-neutral-subdued)] text-[var(--gemba-neutral-signal)]`; `accent` → `bg-[var(--gemba-accent-subdued)] text-[var(--gemba-accent)]`; `success` → `--gemba-success-subdued`/`--gemba-success`; `warning` → `--gemba-warning-subdued`/`--gemba-warning`; `critical` → `--gemba-critical-subdued`/`--gemba-critical`. Default variant `neutral`. Support an optional `icon?: React.ReactNode` prefix rendered in a 16px slot before the label (call sites pass `<Icon name="..." size={16} />` from the wrapper — do NOT hardcode a glyph). Export `Chip` and `chipVariants`. Use only Gemba tokens — no invented colours. (Home page uses only `neutral`/`accent`; author all five for reuse in later phases.)
  </action>
  <verify>
    <automated>grep -qE "export\s+(function\s+Chip|\{[^}]*Chip)" src/components/chip.tsx && grep -qE "chipVariants" src/components/chip.tsx && grep -qE "gemba-accent-subdued" src/components/chip.tsx && grep -qE "gemba-chip-label" src/components/chip.tsx && npx tsc --noEmit 2>&1 | tail -3 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `chip.tsx` exports `Chip` + `chipVariants`; default variant `neutral`.
    - Five variants (neutral/accent/success/warning/critical) map to `--gemba-{signal}-subdued` background + `--gemba-{signal}` text.
    - Dimensions match the reference: 20px tall, 16px radius, 10px 700 ALL-CAPS label (`.gemba-chip-label` + `uppercase`), 4px icon↔label gap.
    - Optional `icon` prefix slot renders a passed node in a 16px box; no glyph hardcoded.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Chip renders ALL-CAPS signal labels on 8% tints with an optional Icon prefix, matching the Gemba Chip spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentational components (variants, class strings, an optional ReactNode slot). No network, data-handling, or user-input surface; the client-side-encryption boundary is untouched. Chip's `icon` slot renders a caller-supplied React node (JSX), not raw HTML — no injection sink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | Chip `icon` prop | accept | Rendered as a normal React child (`{icon}`), never via `dangerouslySetInnerHTML`; React escapes/So no markup injection is possible. |
| T-01-03-SC | Tampering | package installs | n/a | No package-manager installs in this plan (retrofits + one new local file). |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npm run lint` reports no new errors.
- `grep` confirms: Card base has no `border` utility; Button has a `tertiary` variant + `--radius-xl` pills; Chip uses `--gemba-accent-subdued` and `.gemba-chip-label`.
</verification>

<success_criteria>
- COMP-01: Button ranks (Primary/Secondary/Tertiary/Ghost × default/small/square) exist via the retrofitted cva API.
- COMP-05: Card uses the inset-ring + soft-shadow recipe at 16px radius with no CSS border.
- COMP-03: Chip matches the design-system spec (ALL-CAPS label, signal colour on 8% tint).
</success_criteria>

<output>
Create `.planning/phases/01-design-foundation-home-page/01-03-SUMMARY.md` when done, listing the final Button variant/size keys and Chip variant names for downstream call sites.
</output>
