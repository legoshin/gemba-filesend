---
phase: 02-upload-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/file-dropzone.tsx
autonomous: true
requirements: [PAGE-02]

must_haves:
  truths:
    - "The dropzone shows a Gemba dashed border when idle and an accent-tinted fill while dragging"
    - "Selected files render as inset-ring rows with a Gemba file glyph and an accessible remove button"
    - "The dropzone and file list use the Icon wrapper — no lucide-react import remains in file-dropzone.tsx"
    - "Drag-drop, multi-file selection, per-file size-limit rejection, and remove-file continue to work unchanged"
  artifacts:
    - path: "src/components/file-dropzone.tsx"
      provides: "Gemba-reskinned dropzone target + selected-file rows via the Phase 1 Icon wrapper and Card-recipe tokens"
      contains: "@/components/icon"
  key_links:
    - from: "src/components/file-dropzone.tsx"
      to: "@/components/icon"
      via: "Icon wrapper import (replaces lucide-react)"
      pattern: "from \"@/components/icon\""
    - from: "src/components/file-dropzone.tsx"
      to: "removeFile / handleDrop / filterBySize"
      via: "preserved drag-drop + file-management logic (unchanged)"
      pattern: "removeFile"
---

## Phase Goal

**As a** person sharing files, **I want** the upload flow — dropzone, share options, and the generated share link — redesigned to the Gemba design system and theme-aware in light and dark, **so that** sharing a file feels consistent, polished, and trustworthy across the whole app.

_This plan delivers the **dropzone slice** (D-03): after it lands, a user drops files into a fully Gemba-themed drop target and sees selected files as Gemba inset-ring rows — the rest of the page is untouched and still works._

<objective>
Reskin `src/components/file-dropzone.tsx` to the Gemba design system per CONTEXT.md **D-03**: dashed drop target on the Gemba border token (idle) with an accent-tinted active state, and selected-file rows styled with the Phase 1 Card inset-ring recipe. Migrate the three `lucide-react` icons on this file to the Phase 1 `Icon` wrapper.

Purpose: Realize PAGE-02 on the file-selection surface using only Phase 1 components and Gemba tokens (no invented values), without touching any drag-drop / file-management behavior.
Output: A reskinned `file-dropzone.tsx` — visual/token/import changes only; every callback (`handleDrop`, `handleFileSelect`, `removeFile`, `filterBySize`, size-limit toast) preserved byte-for-byte.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The reskin spec (exact class strings + token map) and the decisions
@.planning/phases/02-upload-page-redesign/02-PATTERNS.md
@.planning/phases/02-upload-page-redesign/02-CONTEXT.md
@.planning/phases/01-design-foundation-home-page/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin the drop-target label (D-03 idle/active border + Icon)</name>
  <files>src/components/file-dropzone.tsx</files>
  <read_first>
    - src/components/file-dropzone.tsx (the file — drop-target `<label>` at lines 111-144, and the `CloudUpload` import line 4)
    - .planning/phases/02-upload-page-redesign/02-PATTERNS.md (dropzone border pattern, lines 141-221 — exact idle/active class strings + the `UploadCloud01` glyph + copy text)
    - .planning/phases/02-upload-page-redesign/02-CONTEXT.md (D-03, lines 47-54)
    - src/app/page.tsx (Icon usage + feature-tile recipe analog, lines 41-64)
    - src/components/icon.tsx (Icon wrapper API — `name` + `size` props, spreads `...rest` onto the `<svg>`)
    - src/app/globals.css (token layer: `--border-default`, `--gemba-accent`, `--gemba-accent-subdued`, `--icon-subdued`, `--radius-lg`; `.gemba-body-strong`/`.gemba-body-sm` resolve via design-system/tokens/typography.css)
  </read_first>
  <action>
    Replace the `import { CloudUpload, File, X } from "lucide-react";` line with `import { Icon } from "@/components/icon";`. On the drop-target `<label>` className, keep the layout utilities but swap the shape and color tokens: radius `rounded-xl` becomes `rounded-[var(--radius-lg)]`; keep `border-2 border-dashed`; the idle branch becomes `border-[var(--border-default)] hover:border-[var(--gemba-accent)]/50` and the active (`isDragging`) branch becomes `border-[var(--gemba-accent)] bg-[var(--gemba-accent-subdued)]` — do NOT invent an opacity utility like `bg-primary/5`; `--gemba-accent-subdued` is the canonical 8%-tint token. Replace the `<CloudUpload … className="… h-10 w-10 …">` element with `<Icon name="UploadCloud01" size={40} aria-hidden="true" className={cn("pointer-events-none mb-4", isDragging ? "text-[var(--gemba-accent)]" : "text-[var(--icon-subdued)]")} />`. Change the two copy paragraphs: the primary line to `className="gemba-body-strong pointer-events-none"` and the hint line to `className="gemba-body-sm pointer-events-none mt-1 text-[var(--text-subdued)]"`, keeping their exact text and the `maxSizeMb` expression. Do NOT touch the `<input type="file" …>`, the drag handlers (`onDragEnter`/`onDragLeave`/`onDragOver`/`onDrop`), or any callback. Exact strings are in 02-PATTERNS.md lines 158-221; use them verbatim.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "UploadCloud01" src/components/file-dropzone.tsx && grep -q "gemba-accent-subdued" src/components/file-dropzone.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `import { Icon } from "@/components/icon"` present; `CloudUpload` no longer referenced.
    - Drop-target uses `rounded-[var(--radius-lg)]`, idle `border-[var(--border-default)]`, active `border-[var(--gemba-accent)]` + `bg-[var(--gemba-accent-subdued)]` — no `border-primary`, `bg-primary/5`, `bg-muted/50`, `rounded-xl` remain on the label.
    - Cloud glyph renders via `<Icon name="UploadCloud01" size={40} />`; copy lines use `.gemba-body-strong` / `.gemba-body-sm`.
    - Drag/drop still works: dragging over the label toggles the accent state and dropping still calls `onFilesChange` (handlers unchanged).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Reskin selected-file rows as Card inset-ring rows (D-03 + Icon + aria-label)</name>
  <files>src/components/file-dropzone.tsx</files>
  <read_first>
    - src/components/file-dropzone.tsx (selected-file rows, lines 146-173 — `File` icon, file glyph tile, and the ghost remove `<Button>`)
    - .planning/phases/02-upload-page-redesign/02-PATTERNS.md (selected-file rows pattern, lines 168-202 — exact inset-ring row + `File01`/`XClose` + `size="icon-sm"` + mandatory aria-label)
    - .planning/phases/02-upload-page-redesign/02-CONTEXT.md (D-03, lines 47-54)
    - src/components/ui/card.tsx (inset-ring recipe: `shadow-[var(--ring-border),var(--shadow-card)]`, no `border` class)
    - src/components/ui/button.tsx (variant `ghost` + size `icon-sm` = `size-8 rounded-[var(--radius-sm)]`, line 32)
    - src/app/globals.css (`--surface-card`, `--surface-subdued`, `--ring-border`, `--icon-subdued`, `--text-subdued`, `--radius-md`, `--radius-sm`)
    - .planning/phases/01-design-foundation-home-page/01-UI-SPEC.md (Card recipe guardrail: no Tailwind `border` on card surfaces; icon-only buttons MUST carry aria-label)
  </read_first>
  <action>
    In the `files.length > 0` block, restyle each file row `<div>`: replace `rounded-lg border bg-card p-3` with `flex items-center gap-3 rounded-[var(--radius-md)] bg-[var(--surface-card)] p-3 shadow-[var(--ring-border)]` — use the inset-ring `box-shadow`, never a `border` class (UI-SPEC Card guardrail). Restyle the inner file-glyph tile from `h-9 w-9 … rounded-md bg-muted` to `flex size-9 shrink-0 items-center justify-center rounded-[var(--radius-sm)] bg-[var(--surface-subdued)]`, and replace `<File className="h-4 w-4 text-muted-foreground" />` with `<Icon name="File01" size={16} className="text-[var(--icon-subdued)]" />`. Change the filename `<p>` to `className="gemba-body-strong truncate"` and the size `<p>` to `className="gemba-body-sm text-[var(--text-subdued)]"` (keep the `formatSize(file.size)` call). Replace the remove `<Button variant="ghost" size="icon" className="h-8 w-8 shrink-0" …>` with `<Button variant="ghost" size="icon-sm" className="shrink-0" aria-label={`Remove ${file.name}`} onClick={() => removeFile(index)}>` wrapping `<Icon name="XClose" size={16} />` — the `aria-label` is mandatory (icon-only square button). Do NOT change `removeFile`, `formatSize`, the `.map` key, or any state. Exact strings: 02-PATTERNS.md lines 182-201.
  </action>
  <verify>
    <automated>npx tsc --noEmit && ! grep -q "lucide-react" src/components/file-dropzone.tsx && grep -q "File01" src/components/file-dropzone.tsx && grep -q "aria-label={`Remove" src/components/file-dropzone.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Zero `lucide-react` references remain anywhere in `file-dropzone.tsx` (import removed; `File`, `X`, `CloudUpload` all gone).
    - File rows use `shadow-[var(--ring-border)]` on `bg-[var(--surface-card)]` — no `border`/`bg-card`/`bg-muted`/`rounded-lg` legacy classes remain.
    - Remove button is `variant="ghost" size="icon-sm"` with a per-file `aria-label` and an `<Icon name="XClose" size={16} />`.
    - Removing a file still updates the list (`removeFile` unchanged); `formatSize` output unchanged.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user files → dropzone (client-only) | Selected `File` objects stay in the browser; no network crossing occurs in this component. Unchanged this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-A | Tampering | `filterBySize` per-file size gate | accept | Reskin edits only JSX/className/imports; the `maxSizeBytes` guard and reject toast are preserved byte-for-byte — no change to the client-side validation path. |
| T-02-01-B | Information Disclosure | file names rendered in rows | accept | File names were already displayed; rendering is unchanged (only styling tokens differ). No new data surface. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — the `Icon` wrapper and `Button` already shipped in Phase 1. If any component were added it would be shadcn OFFICIAL only per Phase 1 UI-SPEC Registry Safety. |

**Encryption boundary:** Untouched — this component never handles the encryption key, password, or any network request. The client-side-encryption model (key lives only in the share-link URL fragment) is not reachable from this file. **Must-not-regress:** none of this file's logic participates in key handling.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run lint` reports no new errors on `src/components/file-dropzone.tsx`.
- `grep -c lucide-react src/components/file-dropzone.tsx` returns 0.
- Manual (covered by 02-03 checkpoint): drop target renders dashed idle / accent-tinted active; file rows render as inset-ring rows in light and dark.
</verification>

<success_criteria>
- `file-dropzone.tsx` uses only Gemba tokens + Phase 1 `Icon`/`Button` — no `lucide-react`, no `border-primary`/`bg-muted`/`bg-card` legacy classes (PAGE-02 on the dropzone surface).
- Drag-drop, multi-file add, per-file size rejection, and remove-file behave exactly as before.
</success_criteria>

## Artifacts This Phase Produces (this plan)

- `src/components/file-dropzone.tsx` — reskinned drop target (D-03 dashed/accent) + inset-ring selected-file rows, all icons via the Phase 1 `Icon` wrapper. No new files created.

<output>
Create `.planning/phases/02-upload-page-redesign/02-01-SUMMARY.md` when done.
</output>
