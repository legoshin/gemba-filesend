---
phase: 02-upload-page-redesign
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified: []
autonomous: false
requirements: [PAGE-02]

must_haves:
  truths:
    - "The redesigned upload page renders correctly in both light and dark themes, matching the Phase 1 visual system"
    - "npm run build and eslint pass on the redesigned files with no new errors"
    - "No lucide-react, Badge, or raw legacy styling (text-3xl, text-lg, bg-green-100, border-input) remains on the upload surfaces"
    - "Existing upload functionality (drag-drop, multi-file, client-side encryption, share-link generation) still works end-to-end"
  artifacts:
    - path: "src/app/upload/page.tsx"
      provides: "Verified reskinned upload page (build + visual sign-off)"
      contains: "@/components/icon"
    - path: "src/components/file-dropzone.tsx"
      provides: "Verified reskinned dropzone (build + visual sign-off)"
      contains: "@/components/icon"
  key_links:
    - from: "src/app/upload/page.tsx"
      to: "next-themes .dark layer"
      via: "Gemba tokens resolve to dark values under .dark"
      pattern: "var\\(--"
---

## Phase Goal

**As a** person sharing files, **I want** the upload flow — dropzone, share options, and the generated share link — redesigned to the Gemba design system and theme-aware in light and dark, **so that** sharing a file feels consistent, polished, and trustworthy across the whole app.

_This plan is the **phase gate**: an automated regression/audit pass followed by a human visual sign-off of the fully redesigned upload page in light and dark — mirroring Phase 1's 01-07 gate. It ships no code; it proves the two reskin slices meet PAGE-02 and success criteria 1–3._

<objective>
Verify Phase 2 is complete: run the full build + lint + source audit over the two reskinned files, then obtain human visual sign-off that the upload page (dropzone, share options, share-link result) renders correctly in **light and dark** and that upload still works end-to-end.

Purpose: Success criteria 1–2 are visual and cannot be fully proven by automation; criterion 3 (behavior preserved) needs a live smoke test. This gate closes PAGE-02.
Output: A pass/fail sign-off recorded in the SUMMARY. No source files are modified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02-upload-page-redesign/02-CONTEXT.md
@.planning/phases/01-design-foundation-home-page/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated phase gate — build, lint, and legacy-styling audit</name>
  <files>src/app/upload/page.tsx, src/components/file-dropzone.tsx</files>
  <read_first>
    - src/app/upload/page.tsx (confirm final reskinned state)
    - src/components/file-dropzone.tsx (confirm final reskinned state)
    - .planning/phases/02-upload-page-redesign/02-PATTERNS.md (the target end-state for both files)
    - .planning/phases/01-design-foundation-home-page/01-UI-SPEC.md (Registry Safety + token/component contract to audit against)
  </read_first>
  <action>
    Run the full completeness gate over the two Phase 2 files. (1) `npm run build` must exit 0 (Next.js production build — catches broken imports/JSX across the app). (2) `npm run lint` must report no new errors on `src/app/upload/page.tsx` or `src/components/file-dropzone.tsx`. (3) Legacy-styling audit — each of these must return zero matches: `grep -rn "lucide-react" src/app/upload/page.tsx src/components/file-dropzone.tsx`; `grep -rn "ui/badge" src/app/upload/page.tsx`; `grep -rnE "text-3xl|text-lg|bg-green-100|border-input|bg-primary/5|bg-muted" src/app/upload/page.tsx src/components/file-dropzone.tsx`. (4) Positive audit — confirm the Phase 1 components are wired: `grep -q "@/components/icon"` and `grep -q "@/components/chip"` in `upload/page.tsx`, and `grep -q "@/components/icon"` in `file-dropzone.tsx`. (5) Encryption-boundary regression check — confirm the share-link fragment construction and crypto imports survived: `grep -q "#\${keyB64}" src/app/upload/page.tsx` (or the exact template-literal form present) and `grep -q "@/lib/crypto" src/app/upload/page.tsx`. Record every command's result in the SUMMARY. If any check fails, STOP and report — do not proceed to the human checkpoint.
  </action>
  <verify>
    <automated>npm run build && npm run lint && ! grep -rnE "lucide-react|text-3xl|bg-green-100|border-input|bg-primary/5" src/app/upload/page.tsx src/components/file-dropzone.tsx && grep -q "@/lib/crypto" src/app/upload/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; `npm run lint` shows no new errors on the two files.
    - Legacy-styling audit returns zero matches for `lucide-react`, `ui/badge`, `text-3xl`, `text-lg`, `bg-green-100`, `border-input`, `bg-primary/5`, `bg-muted`.
    - Positive audit confirms `@/components/icon` (both files) and `@/components/chip` (page) are imported.
    - Encryption boundary intact: share-link fragment construction and `@/lib/crypto` imports are still present in `upload/page.tsx`.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual sign-off — upload page in light + dark</name>
  <what-built>
    The upload page (`/upload`) and its dropzone reskinned to the Gemba design system: dashed/accent dropzone + inset-ring file rows (D-03), two-card compose layout with Gemba share-option controls incl. the in-place native select (D-01/D-02), reskinned progress + primary CTA, and a calm success/share-link state with Chips (D-04). All icons via the Icon wrapper; all values from Gemba tokens.
  </what-built>
  <how-to-verify>
    1. Start the app: `npm run dev`, open `http://localhost:3000/upload`.
    2. LIGHT theme — confirm: header uses the Gemba heading scale (not bold `text-3xl`); both cards use the white inset-ring surface (hairline ring + soft shadow, no hard border); the dropzone shows a dashed border on the Gemba border color.
    3. Drag a file over the dropzone — confirm the border turns accent blue with a subtle 8% tint fill; drop it — confirm the file appears as an inset-ring row with a file glyph and a remove (X) button; hover/click remove works.
    4. Toggle Password Protection ON — confirm the Switch, password field, download-limit and expiry inputs, and the expiry-unit dropdown all share the same 40px height, radius, hairline ring, and ink focus ring (the native select must match its sibling inputs).
    5. Click Upload — confirm the progress state renders with Gemba type/track, then the "done" state shows a small restrained success check (not a large green block), the share link in a readonly field with a square copy button, and the summary as ALL-CAPS Chips (password/downloads/expiry). Click the copy button — confirm it copies and shows the check state. Click "Upload More" — confirm reset.
    6. Switch to DARK theme (theme toggle) and repeat steps 2–5 — confirm every element re-themes cleanly on the near-black surface: no white/unstyled boxes, the accent tint and success check use the lightened dark variants, text stays legible, focus rings visible.
    7. Multi-file: select 2+ files and upload — confirm one Chip summary and one readonly link per file, and "Copy All Links".
  </how-to-verify>
  <resume-signal>Type "approved" if light + dark both look correct and upload works end-to-end, or describe any visual/behavior issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (verification only) | This plan runs build/lint/grep and a manual smoke test. It modifies no source and crosses no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-A | Tampering | encryption-boundary regression | mitigate | Task 1 explicitly greps that the share-link URL-fragment construction and `@/lib/crypto` imports survived the reskin (must-not-regress). The human smoke test confirms a real upload still produces a working `#<key>` share link. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No installs — verification-only plan; no dependencies added. |

**Encryption boundary:** This gate is the explicit checkpoint that the client-side-encryption model (key only in the URL fragment, never sent to the server) and password hashing/salt handling did not regress during the reskin.
</threat_model>

<verification>
- Task 1 automated gate passes (build + lint + audit + boundary check).
- Human sign-off recorded: light PASS, dark PASS, upload end-to-end PASS.
- If any issue is reported, it is logged as a gap for a follow-up gap-closure plan (do not silently pass).
</verification>

<success_criteria>
- PAGE-02 satisfied: the entire upload flow (dropzone, share options, share link) uses Gemba tokens + Phase 1 components, verified visually in light and dark.
- Success criterion 3 confirmed live: drag-drop, multi-file, client-side encryption, and share-link generation work unchanged.
</success_criteria>

## Artifacts This Phase Produces (this plan)

- No source files. Produces the phase sign-off record in `02-03-SUMMARY.md` (build/lint/audit results + human light/dark verdict). Gates completion of PAGE-02.

<output>
Create `.planning/phases/02-upload-page-redesign/02-03-SUMMARY.md` when done.
</output>
