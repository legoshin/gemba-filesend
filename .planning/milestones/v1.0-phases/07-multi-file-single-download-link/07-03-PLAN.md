---
phase: 07-multi-file-single-download-link
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/download/page.tsx
autonomous: false
requirements: [MFL-04, MFL-05]
estimate:
  tokens: 62000
  raw_tokens: 41000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The download page lists every file in a share (name + size) and downloads/decrypts each file individually with the one shared key from the link fragment (per MFL-04)."
    - "A legacy single-file share (meta with no files[]) still previews, downloads, and decrypts exactly as before (per MFL-05)."
    - "Password + recipient-verification gates apply once to the whole share and still block download until satisfied."
    - "The whole-share download decrements the one-per-id counter once, not once per file (client sends consume=1 for the first file only)."
  artifacts:
    - "src/app/download/page.tsx — file-list preview + per-file download/decrypt loop handling multi-file and legacy shapes"
  key_links:
    - "meta.files[] (or legacy single derived from top-level fields) drives the file list; the single #fragment key decrypts every file"
    - "per-file bytes fetched via GET /api/files/{id}?index=N&consume=0|1 then decryptPacked with the shared key -> browser download"
  prohibitions:
    - "NO requiring a separate key per file: one importKeyBase64(key) decrypts all files."
    - "NO zip/bundle download path: files are fetched + decrypted + saved individually."
    - "Do NOT break legacy single-file share download/decrypt or the password/verify gates."
    - "Do NOT decrement the download counter once per file (once per whole-share download)."
---

<objective>
Rewrite the download page to list every file in a share and download/decrypt each individually with the single key from the link fragment, while keeping legacy single-file shares working. Passwords and recipient verification still gate the whole share.

Purpose: Delivers the recipient side of "many files, one link" (MFL-04) and closes the backward-compatibility requirement (MFL-05), consuming the meta.files[] + index-addressable bytes route from Plan 01.
Output: A multi-file preview + per-file download/decrypt flow with legacy fallback, verified end-to-end by a human check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-multi-file-single-download-link/07-CONTEXT.md
@.planning/phases/07-multi-file-single-download-link/07-01-SUMMARY.md
@src/lib/crypto.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch meta → render file list (multi-file + legacy shapes)</name>
  <files>src/app/download/page.tsx</files>
  <read_first>
    - src/app/download/page.tsx (FileInfo interface, isMetaPayload guard, fetchFileInfo, preview render ~lines 29-183 and the preview Card — the file being modified)
    - .planning/phases/07-multi-file-single-download-link/07-01-SUMMARY.md (meta response now includes files[])
  </read_first>
  <action>
    Extend the meta type guard and FileInfo so the preview describes a list of files. Widen isMetaPayload to accept an OPTIONAL files array of { name, type, size } while still requiring the existing top-level fields (a legacy meta with no files array must still pass). In fetchFileInfo, derive the file list as data.files when present and non-empty, otherwise a single-element list from the top-level name/type/size (legacy) — store it on FileInfo (e.g. files: Array<{ name; type; size; sizeLabel }>) alongside the existing single id/keyBase64/downloadsRemaining/expiresIn/passwordProtected/verifyRequired/encrypted (which stay per-share). Update the preview Card to list every file (name + formatSize) reusing the existing File01 icon row pattern; keep the single share-level chips (downloads-left, expiry, password/verify) describing the whole share. Import ONE key from the fragment as today (keyBase64) — do not require a key per file.
  </action>
  <acceptance_criteria>
    - isMetaPayload accepts a meta WITH a files array and a legacy meta WITHOUT one (both pass) — assert the files check is optional.
    - FileInfo carries a files list; the preview renders one row per file (name + size) via the existing File01 row/formatSize (`grep -q "files" src/app/download/page.tsx`).
    - A single keyBase64 is read from the fragment (no per-file key state added).
    - `npx tsc --noEmit` clean and `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -20 && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>The preview lists every file in a share (multi-file and legacy) with one shared key and one set of share-level chips.</done>
</task>

<task type="auto">
  <name>Task 2: Per-file download + decrypt with the shared key (multi-file + legacy)</name>
  <files>src/app/download/page.tsx</files>
  <read_first>
    - src/app/download/page.tsx (handleDownload ~lines 254-400, the presign/stream + decryptPacked flow — the file being modified)
    - src/lib/crypto.ts (importKeyBase64/decryptPacked — REUSE)
    - .planning/phases/07-multi-file-single-download-link/07-01-SUMMARY.md (GET /api/files/{id}?index=N&consume=0|1 contract + counter policy)
  </read_first>
  <action>
    Generalize handleDownload into a per-file download that takes an index: fetch bytes via GET /api/files/{id}?index={i}&consume={c} (keep the existing x-password / x-verify-token headers and the blob-mode two-step presign→CDN fetch vs fs-mode direct-stream handling, the 28-byte/truncation guards, and the progress + error mapping for 401/403/404/410/503), then importKeyBase64(fileInfo.keyBase64) ONCE (or per call, same key) and decryptPacked the bytes, then trigger the browser save using that file's name/type. Add per-file download buttons in the file list and a "Download all" affordance that iterates the files sequentially (Claude's discretion for UX). Counter policy (REL-01): send consume=1 on exactly the FIRST file fetched in a download action and consume=0 on the remaining files, so the whole-share download decrements the one-per-id counter once — document this in the SUMMARY. For a legacy single-file share (files length 1, index 0) the flow is unchanged (default consume decrements once). Preserve the "unencrypted fallback" path (no decrypt when encrypted is false). Refresh downloadsRemaining display after a completed share download.
  </action>
  <acceptance_criteria>
    - The bytes fetch targets `/api/files/${id}?index=` with a consume param (`grep -qE "index=|consume=" src/app/download/page.tsx`).
    - One importKeyBase64(fileInfo.keyBase64) decrypts every file (no second key per file introduced).
    - A "Download all" iterates files and per-file buttons exist; consume=1 is sent for the first file only in a download action (assert the first-file/rest distinction).
    - Legacy single-file (files length 1) downloads + decrypts unchanged; password/verify gates still block until satisfied and 401/403/404/410/503 error mapping is preserved.
    - `npx tsc --noEmit` clean; `npm run build` succeeds; `npm test` green.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -20 && npm run build 2>&1 | tail -15 && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>Each file in a share downloads and decrypts individually under the one shared key; download-all works; legacy single-file shares still download/decrypt; the counter decrements once per whole-share download.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — multi-file share end to end + legacy compat</name>
  <what-built>Multi-file single-download-link end to end: upload N files → one share link; recipient page lists all files and downloads/decrypts each with the one key; legacy single-file link still works; notify sends one link.</what-built>
  <how-to-verify>
    Run the app locally (`npm run dev`, fs storage mode — no Upstash/Blob creds needed):
    1. Go to /upload, select 2+ files, upload. Confirm the success screen shows ONE link and lists all selected files (not one link per file).
    2. Open the single link in a new tab. Confirm the download page lists every file with name + size and one set of share-level info (downloads left / expiry).
    3. Download each file (and "Download all"); confirm each opens/decrypts correctly (contents intact) and that the whole-share download consumed ONE download from the limit, not one per file.
    4. Backward compatibility: open a previously-created single-file share link (or one made before this phase); confirm it still previews, downloads, and decrypts.
    5. Optional: enable password and/or recipient notify on an upload; confirm the gate blocks until satisfied and (if notify) recipients get ONE link.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what failed (e.g. a file didn't decrypt, counter decremented per file, or legacy link broke).</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (download UI)
- File-list preview in `src/app/download/page.tsx` handling both multi-file (`meta.files[]`) and legacy single-file shapes.
- Per-file download/decrypt loop using one `importKeyBase64(key)` for all files, via `GET /api/files/{id}?index=N&consume=0|1`.
- "Download all" affordance; whole-share download decrements the counter once.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| link fragment → browser | the one key is read from `#` and used to decrypt every file locally |
| browser → /api/files/{id} | untrusted index/consume + password/verify headers cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Elevation of privilege | per-file consume flag (over-download) | high | mitigate | Client sends consume=1 for the first file only; server still enforces the non-decrementing remaining>0 gate on consume=0 (Plan 01 T-07-05) |
| T-07-10 | Information disclosure | key handling on decrypt | critical | mitigate | Key read only from the URL fragment; used in-browser for decryptPacked; never sent to the server |
| T-07-11 | Tampering | legacy link regression | medium | mitigate | Legacy single-file (files length 1 / no files[]) path preserved and covered by the human-verify step 4 |
</threat_model>

<verification>
- Multi-file link lists all files; each downloads + decrypts with the one key.
- Legacy single-file link still downloads + decrypts.
- Whole-share download decrements the counter once (not per file).
- `npx tsc --noEmit` clean; `npm run build` succeeds; `npm test` green.
- Human-verify checkpoint approved.
</verification>

<success_criteria>
The recipient sees every file in a share, downloads/decrypts each with the single shared key, legacy single-file shares still work, gates hold, and the counter is one-per-share — confirmed by build, type-check, tests, and the human-verify end-to-end check.
</success_criteria>

<output>
Create `.planning/phases/07-multi-file-single-download-link/07-03-SUMMARY.md` when done.
</output>
