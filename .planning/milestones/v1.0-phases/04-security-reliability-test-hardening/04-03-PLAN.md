---
phase: 04-security-reliability-test-hardening
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - vitest.config.ts
  - package.json
  - package-lock.json
  - src/lib/crypto.test.ts
  - src/lib/storage.test.ts
autonomous: false
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]
must_haves:
  truths:
    - "`npm test` runs Vitest and exits 0 (Vitest is the project's first test runner — D-11)"
    - "The crypto encrypt/decrypt round-trip is asserted and the packed format is verified as [12-byte IV][ciphertext+auth tag] (TEST-01, D-12/D-14)"
    - "Password hashing/validation is asserted: sha256Hex(password+salt) matches on the correct password and mismatches on a wrong password (TEST-02, D-14)"
    - "Metadata validation is asserted: validateClientMeta accepts a valid payload and rejects each invalid branch; StoredMeta survives a JSON round-trip (TEST-04)"
    - "The REL-01 concurrency fix is asserted: N parallel decrementDownloadCounter calls against a seeded dl:{id} on an in-memory Redis fake never exceed the limit and never let more than `limit` decrements succeed (TEST-03, D-13)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config (node test environment, no polyfill)"
      min_lines: 5
    - path: "src/lib/crypto.test.ts"
      provides: "TEST-01 + TEST-02 coverage against Node built-in Web Crypto"
      min_lines: 30
    - path: "src/lib/storage.test.ts"
      provides: "TEST-04 metadata coverage + TEST-03 counter/concurrency coverage via in-memory Redis fake"
      min_lines: 40
    - path: "package.json"
      provides: "test script"
      contains: "\"test\""
  key_links:
    - from: "src/lib/storage.test.ts"
      to: "decrementDownloadCounter (from Plan 04-02 redis.ts)"
      via: "injected in-memory Redis fake { set, decr }"
      pattern: "decrementDownloadCounter"
    - from: "src/lib/crypto.test.ts"
      to: "encryptPacked / decryptPacked / sha256Hex"
      via: "direct import from @/lib/crypto (Node globalThis.crypto.subtle, no polyfill)"
      pattern: "encryptPacked"
---

## Phase Goal

**As a** maintainer relying on Gemba Filesend, **I want to** have the crypto, password, counter, and metadata logic covered by automated tests, **so that** the encryption round-trip and the download-limit race fix stay provably correct as the app evolves.

<objective>
Ship TEST-01..04: establish Vitest as the project's first test runner (D-11) and add regression/characterization tests for the crypto round-trip + packed format (TEST-01), password hash/validate (TEST-02), the download-counter decrement + limit enforcement including the REL-01 concurrency fix (TEST-03), and metadata serialization/validation (TEST-04).

These are regression tests over already-shipped (crypto/metadata) and just-shipped (Plan 04-02 counter) behavior — they are expected to pass green, and they lock the behavior in. TEST-03 is the direct regression test for the REL-01 fix and therefore depends on Plan 04-02's `decrementDownloadCounter`.

Purpose: satisfy phase success criterion 4 (automated unit tests exist and pass) and give the security/reliability work a durable safety net.
Output: `vitest.config.ts`, a `test` npm script, `src/lib/crypto.test.ts`, `src/lib/storage.test.ts`.

Decisions implemented: D-11 (Vitest + test script), D-12 (crypto tests run against Node's built-in globalThis.crypto.subtle — no polyfill), D-13 (in-memory Redis fake for the hermetic concurrency test), D-14 (test the CURRENT crypto: AES-128-GCM + SHA-256 — NO AES-256/PBKDF2 migration tests).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-security-reliability-test-hardening/04-CONTEXT.md
@.planning/phases/04-security-reliability-test-hardening/04-PATTERNS.md
@src/lib/crypto.ts
@src/lib/storage.ts
@src/lib/redis.ts
@src/app/api/files/route.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>About to add the project's first test tooling as devDependencies: `vitest` and `@vitest/coverage-v8` (D-11). No RESEARCH.md / Package Legitimacy Audit exists for this phase (research disabled), so per the fallback policy both are treated as [ASSUMED] and must be human-verified before install. Blocking supply-chain gate (never auto-approvable).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/vitest — confirm the official Vitest package (part of the vitest-dev/vitest monorepo), current major version, very high weekly downloads.
    2. Open https://www.npmjs.com/package/@vitest/coverage-v8 — confirm official `@vitest` scope, version aligned with the vitest version.
    3. Confirm neither is a typosquat (exact names `vitest` / `@vitest/coverage-v8`).
    4. On approval, the executor runs `npm install -D vitest @vitest/coverage-v8`, then adds `"test": "vitest run"` to package.json scripts (alongside dev/build/lint) and confirms package.json + package-lock.json updated.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize installing vitest + @vitest/coverage-v8, or "reject" to stop.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest setup + crypto round-trip / packed-format / password tests (TEST-01, TEST-02)</name>
  <files>vitest.config.ts, package.json, src/lib/crypto.test.ts</files>
  <read_first>
    - src/lib/crypto.ts (FULL — encryptPacked lines 31-46 returns [IV(12)][ciphertext+tag]; decryptPacked lines 48-59 with the "payload too short" guard; sha256Hex lines 61-67; randomSaltBase64 lines 69-71; toBase64Url/fromBase64Url lines 73-86)
    - src/app/api/files/[id]/route.ts (lines 24-35: checkPassword — the exact `sha256Hex(provided + salt)` composition that TEST-02 characterizes as the "spec" for correct password usage)
    - src/app/api/files/route.ts (lines 89-94: how salt + passwordHash are produced at upload — the other half of the password spec)
    - .planning/phases/04-security-reliability-test-hardening/04-PATTERNS.md §vitest.config.ts + §src/lib/crypto.test.ts (config conventions + co-located-test rule; D-12 no polyfill)
    - next.config.ts, postcss.config.mjs (root config-file conventions: minimal, single default export, ESM)
  </read_first>
  <behavior>
    - crypto round-trip: decryptPacked(encryptPacked(data, key), key) equals the original bytes for several payload sizes (empty-ish, small, multi-KB).
    - packed format: encryptPacked output length is 12 (IV) + input length + 16 (GCM tag); the first 12 bytes are the IV; two encryptions of the same input differ (random IV).
    - decryptPacked rejects a payload of length <= 12 with "payload too short".
    - decryptPacked with the wrong key / tampered ciphertext throws (auth tag failure).
    - password: for salt = randomSaltBase64(), sha256Hex(password + salt) is stable, matches on the correct password, and differs on a wrong password (mirrors checkPassword's composition).
  </behavior>
  <action>
    Create `vitest.config.ts` at repo root: minimal single default export via `defineConfig` from `vitest/config`; set `test.environment` to `"node"` (server-side crypto/redis logic — NO jsdom, NO Web Crypto polyfill per D-12, since Node 18+ provides globalThis.crypto.subtle natively) and `test.include` to `["src/**/*.test.ts"]`. Follow the next.config.ts/postcss.config.mjs root-config style.

    Confirm `package.json` scripts has `"test": "vitest run"` (added on checkpoint approval); if missing, add it alongside dev/build/lint.

    Create `src/lib/crypto.test.ts` co-located next to crypto.ts. Import the functions under test from `@/lib/crypto`. Use Vitest `describe`/`it`/`expect`. Cover TEST-01 and TEST-02 per the behavior block above. Generate keys with the module's own `generateKey()` (real Web Crypto). Do not mock crypto. Keep tests deterministic except where randomness is the property under test (IV uniqueness, salt-driven hash difference).
  </action>
  <acceptance_criteria>
    - `grep -F "environment" vitest.config.ts` shows `"node"`; `grep -F "src/**/*.test.ts" vitest.config.ts` matches; vitest.config.ts has a single default export.
    - `grep -F "\"test\"" package.json` matches (script present).
    - `grep -F "encryptPacked" src/lib/crypto.test.ts` and `grep -F "decryptPacked" src/lib/crypto.test.ts` and `grep -F "sha256Hex" src/lib/crypto.test.ts` all match.
    - crypto.test.ts contains NO polyfill import (no `@peculiar/webcrypto`, no `crypto.subtle` shim) — D-12.
    - `npx vitest run src/lib/crypto.test.ts` exits 0 with all tests passing and at least one test each for: round-trip, packed length/IV, IV uniqueness, too-short rejection, wrong-key throw, password match/mismatch.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/crypto.test.ts && grep -F "\"test\"" package.json && grep -F "environment" vitest.config.ts</automated>
  </verify>
  <done>Vitest is configured (node env, no polyfill), the `test` script exists, and crypto.test.ts covers TEST-01 (round-trip + packed format) and TEST-02 (password hash/validate) with `npx vitest run src/lib/crypto.test.ts` green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Metadata validation tests + REL-01 counter/concurrency test via in-memory Redis fake (TEST-04, TEST-03)</name>
  <files>src/lib/storage.test.ts</files>
  <read_first>
    - src/app/api/files/route.ts (lines 15-17 the MAX_DOWNLOADS/MAX_EXPIRY_MS/MAX_BLOB_BYTES bounds; lines 39-65 validateClientMeta — every branch TEST-04 must exercise)
    - src/lib/storage.ts (lines 10-23 StoredMeta — the shape for the JSON round-trip test)
    - src/lib/redis.ts (from Plan 04-02: seedDownloadCounter / decrementDownloadCounter signatures and their optional injectable `client` param with the minimal `{ set, decr }` surface — this is what makes the hermetic fake possible)
    - .planning/phases/04-security-reliability-test-hardening/04-PATTERNS.md §src/lib/storage.test.ts (TEST-04 branch-coverage guidance + TEST-03 in-memory-fake concurrency guidance, D-13)
  </read_first>
  <behavior>
    - validateClientMeta returns true for a well-formed payload (valid name/type/size within bounds, downloadsRemaining in [1, MAX_DOWNLOADS], future expiresAt within MAX_EXPIRY_MS).
    - validateClientMeta returns false for each violated branch: non-string name/type, size <= 0, size > MAX_BLOB_BYTES, downloadsRemaining < 1, downloadsRemaining > MAX_DOWNLOADS, expiresAt in the past, expiresAt beyond MAX_EXPIRY_MS, and wrong-typed fields.
    - StoredMeta survives JSON.parse(JSON.stringify(meta)) preserving every field (including optional passwordHash/salt/blobUrl when present and absent).
    - Counter: seedDownloadCounter(id, N, ttl, fake) sets dl:{id}=N; decrementDownloadCounter returns N-1, N-2, … and goes negative after N calls.
    - Concurrency (the REL-01 regression): seed dl:{id}=N on the fake, fire M > N parallel decrementDownloadCounter calls via Promise.all, assert exactly N results are >= 0 (allowed) and the rest are < 0 (rejected) — the limit is NEVER exceeded, the counter never lets more than N succeed.
  </behavior>
  <action>
    Create `src/lib/storage.test.ts` co-located in src/lib. 

    For TEST-04: import `validateClientMeta` (export it from `src/app/api/files/route.ts` if it is not already exported — add a named `export` to the existing function; do not otherwise change its logic) and `StoredMeta` from `@/lib/storage`. Write table-style cases enumerating each branch per the behavior block. Add a serialization round-trip test over a representative StoredMeta (one with password fields, one without).

    For TEST-03: build a small in-memory Redis fake — a plain object/class backed by a Map implementing exactly the surface the counter uses: `set(key, value, opts?)` (honoring `{ nx: true }` = only set if absent) and `decr(key)` (initialize missing key to 0 then subtract 1, return the new number). Import `seedDownloadCounter` + `decrementDownloadCounter` from `@/lib/redis` and pass the fake as the injectable `client` argument (this is why Plan 04-02 kept `client` as the last param — do not use a live Upstash instance; D-13 hermetic). Assert the single-threaded decrement sequence, then the concurrency property with `Promise.all` over M > N parallel decrements: count results >= 0 and assert it equals N (never more), and assert no result exceeds N-1 and the minimum is >= -(M-N). Prefer a fresh fake per test for isolation.
  </action>
  <acceptance_criteria>
    - `grep -F "validateClientMeta" src/lib/storage.test.ts` matches; the test file exercises at least 8 distinct invalid branches plus the valid case.
    - `grep -F "decrementDownloadCounter" src/lib/storage.test.ts` and `grep -F "seedDownloadCounter" src/lib/storage.test.ts` match; `grep -F "Promise.all" src/lib/storage.test.ts` matches (the parallel concurrency test).
    - storage.test.ts uses an in-memory fake and does NOT import a live Upstash client / does NOT require UPSTASH_REDIS_REST_URL to run (hermetic — D-13).
    - The concurrency test seeds N and fires M > N parallel decrements and asserts exactly N succeed (>= 0) — encoded as an explicit `expect(...).toBe(N)` on the allowed count.
    - `npx vitest run src/lib/storage.test.ts` exits 0; `npm test` exits 0 (whole suite green).
    - `validateClientMeta` is exported from src/app/api/files/route.ts (named export) with no behavioral change to its predicate.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/storage.test.ts && grep -F "Promise.all" src/lib/storage.test.ts && grep -F "decrementDownloadCounter" src/lib/storage.test.ts && npm test</automated>
  </verify>
  <done>storage.test.ts covers TEST-04 (validateClientMeta branch coverage + StoredMeta JSON round-trip) and TEST-03 (hermetic in-memory-fake concurrency test proving N parallel decrements never exceed the seeded limit); `npm test` runs the full Vitest suite green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04-03)

- **File (new):** `vitest.config.ts` — node test environment, `include: ["src/**/*.test.ts"]`, no Web Crypto polyfill (D-12).
- **File (new):** `src/lib/crypto.test.ts` — TEST-01 (encrypt/decrypt round-trip + [12-byte IV][ciphertext+tag] packed format) and TEST-02 (sha256Hex password hash/validate).
- **File (new):** `src/lib/storage.test.ts` — TEST-04 (validateClientMeta branch coverage + StoredMeta JSON round-trip) and TEST-03 (in-memory Redis fake, N parallel `decrementDownloadCounter` calls never exceed the seeded `dl:{id}` limit).
- **File (modified):** `package.json` — adds `"test": "vitest run"` script + `vitest` / `@vitest/coverage-v8` devDependencies.
- **File (modified):** `src/app/api/files/route.ts` — `validateClientMeta` promoted to a named export so TEST-04 can import it (no logic change).
- **npm script:** `test` (project's first test runner baseline — D-11).
- **New devDeps:** `vitest`, `@vitest/coverage-v8`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → crypto/metadata modules | Tests import and exercise real leaf modules (no network, no secrets) |
| Test process → counter primitive | Tests inject an in-memory fake instead of live Upstash (hermetic) |
| CI/dev machine → npm registry | Installing the test tooling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering (supply chain) | npm install vitest + @vitest/coverage-v8 | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifying both on npmjs.com before install; never auto-approvable. |
| T-04-13 | Information Disclosure (tests hitting live Redis / leaking creds) | TEST-03 counter test | mitigate | Hermetic in-memory fake injected via the `client` param; tests never require UPSTASH_* env and never open a network connection (Task 3, D-13). |
| T-04-14 | Repudiation (false-green tests that don't actually assert the invariant) | TEST-03 concurrency assertion | mitigate | The concurrency test asserts an explicit `toBe(N)` on the allowed count under M > N parallel decrements — a real regression guard for REL-01, not a smoke test (Task 3). |
| T-04-15 | Tampering (polyfill masking real crypto behavior) | TEST-01/02 crypto tests | accept | D-12: run against Node's built-in globalThis.crypto.subtle with no polyfill, so tests exercise the same primitive the app uses in production. |

Supply-chain (T-04-12) has a blocking-human mitigation task; no HIGH threat is left unmitigated.
</threat_model>

<verification>
- `npm test` exits 0 (full Vitest suite green — phase success criterion 4).
- Per-file: `npx vitest run src/lib/crypto.test.ts` and `npx vitest run src/lib/storage.test.ts` both exit 0.
- Source assertions: node env + no polyfill in vitest.config.ts / crypto.test.ts; `Promise.all` + injected fake + explicit `toBe(N)` allowed-count assertion in storage.test.ts; `test` script present in package.json.
</verification>

<success_criteria>
Phase success criterion 4 is met: automated unit tests exist and pass for the crypto encrypt/decrypt round-trip and packed format (TEST-01), password hashing/validation (TEST-02), download-counter decrement + limit enforcement including the REL-01 concurrency fix (TEST-03), and metadata serialization/validation (TEST-04) — all runnable via `npm test`, hermetic, against the current AES-128-GCM + SHA-256 crypto.
</success_criteria>

<output>
Create `.planning/phases/04-security-reliability-test-hardening/04-03-SUMMARY.md` when done.
</output>
