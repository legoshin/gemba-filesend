---
phase: 10-component-reshape
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/button.tsx
  - src/lib/motion.ts
  - src/lib/motion.test.ts
  - design-system/MOTION.md
autonomous: true
requirements: [BTN-01]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every Button rank/variant/size renders with SmoothUI press + hover motion and the app still builds (BTN-01)"
    - "Button motion degrades to no-scale under prefers-reduced-motion (resolved via useMotionPreset, not inline literals)"
    - "src/lib/motion.ts exports variants.focusPop and getSlideOffset(side), both documented in design-system/MOTION.md, so the Forms and Sheet consumers in later waves draw from the foundation (no per-component magic numbers)"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "SmoothUI-reshaped Button (tracer) — motion.create hoist + useMotionPreset, all CVA ranks/sizes preserved"
      contains: "useMotionPreset"
    - path: "src/lib/motion.ts"
      provides: "variants.focusPop pair + getSlideOffset(side) helper"
      contains: "getSlideOffset"
    - path: "src/lib/motion.test.ts"
      provides: "Unit coverage for focusPop reduced-motion contract + getSlideOffset 4-direction output"
      contains: "getSlideOffset"
    - path: "design-system/MOTION.md"
      provides: "Documented focusPop variant-pair row + Helper functions subsection for getSlideOffset"
      contains: "getSlideOffset"
  key_links:
    - from: "src/components/ui/button.tsx"
      to: "src/lib/use-motion-preset.ts"
      via: "useMotionPreset(variants.tapPress/hover, transitions.micro) resolves press/hover props"
      pattern: "useMotionPreset"
    - from: "src/lib/motion.test.ts"
      to: "src/lib/motion.ts"
      via: "imports getSlideOffset + variants to assert the two foundation additions"
      pattern: "getSlideOffset"
---

<objective>
Lead the phase with a tracer that proves the Phase 9 foundation composes into the SmoothUI feel on the highest-traffic primitive — **Button (BTN-01)** — end-to-end with `npm run build` green, BEFORE any Radix-portal work fans out. Then land the exactly-two foundation additions RESEARCH identified (`variants.focusPop`, `getSlideOffset(side)`) so the Forms wave (Input) and the Portal-surfaces wave (Sheet) draw them from the centralized module instead of inlining magic numbers.

Purpose: De-risk the whole phase on the agent's best early-context tokens (Button has the most call sites: home, upload, download, DialogFooter, AppShell, file-dropzone, theme-toggle) and unblock the two later consumers with their required presets.
Output: A reshaped Button, two new foundation exports with unit tests, and their MOTION.md documentation.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-component-reshape/10-CONTEXT.md
@.planning/phases/10-component-reshape/10-RESEARCH.md
@.planning/phases/10-component-reshape/10-PATTERNS.md
@design-system/MOTION.md
@src/components/chip.tsx
@src/lib/motion.ts
@src/lib/shape.ts
@src/lib/use-motion-preset.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Reshape Button end-to-end onto the chip.tsx pattern (BTN-01)</name>
  <files>src/components/ui/button.tsx</files>
  <read_first>src/components/ui/button.tsx (full, 66 lines — current state: no "use client", no motion), src/components/chip.tsx (the proven template — motion.create hoist + NativeSpanProps Omit + useMotionPreset), src/lib/motion.ts (transitions.micro, variants.tapPress/hover), src/lib/use-motion-preset.ts</read_first>
  <action>Reshape `src/components/ui/button.tsx` following `chip.tsx` verbatim as the template. Add `"use client"` at the top and the three foundation imports (`motion` from "motion/react"; `transitions, variants` from "@/lib/motion"; `useMotionPreset` from "@/lib/use-motion-preset"). Hoist BOTH wrapped identities at module scope with the same explanatory comment chip.tsx uses: `MotionButton = motion.create("button")` and `MotionSlot = motion.create(Slot.Root)` — never call motion.create inside the component body (react-hooks/static-components; Phase 9 CR-01 lesson). Inside `Button`, set `const Comp = asChild ? MotionSlot : MotionButton`, resolve `const press = useMotionPreset(variants.tapPress, transitions.micro)` and `const hover = useMotionPreset(variants.hover, transitions.micro)`, and spread `{...press} {...hover}` onto `Comp` (chip.tsx's spread style — the resolved objects carry only whileTap / whileHover keys). Keep the ENTIRE `buttonVariants` CVA block byte-for-byte (all 7 variants + 8 sizes), all `data-slot`/`data-variant`/`data-size` attributes, and the `asChild` single-child contract exactly (Pitfall 3 — do not add conditional-rendering branches to the JSX). Follow chip.tsx and add a `NativeButtonProps` type that `Omit`s the Motion-colliding native handlers (`onDrag`, `onDragStart`, `onDragEnd`, `onAnimationStart`, `onAnimationEnd`, `onAnimationIteration`) from `React.ComponentProps<"button">` before intersecting with `VariantProps` + `asChild`, so the component type-checks under strict mode. The existing `focus-visible:shadow-[var(--ring-focus)]` in the base CVA string already equals `shape.ringFocus` — leave it; make NO colour/type token edits and change no radius values (the size ranks already use `rounded-[var(--radius-*)]`). Reduced motion degrades for free via the resolved presets.</action>
  <verify>
    <automated>grep -q '"use client"' src/components/ui/button.tsx && grep -q 'motion.create' src/components/ui/button.tsx && grep -q 'useMotionPreset' src/components/ui/button.tsx && grep -q 'tapPress' src/components/ui/button.tsx && npm run build && test -z "$(git status --porcelain -- design-system/tokens/)"</automated>
    <fails_when>Button is missing the client directive, the hoisted motion.create identity, the useMotionPreset wiring, or the tapPress preset reference; OR `npm run build` errors (a broken reshape breaks every page/DialogFooter/AppShell that renders Button); OR any colour/type token file under design-system/tokens/ was modified.</fails_when>
  </verify>
  <acceptance_criteria>
    - button.tsx starts with "use client" and imports motion + useMotionPreset + transitions/variants from the Phase 9 foundation.
    - `motion.create("button")` and `motion.create(Slot.Root)` are hoisted at module scope (not called in render).
    - Press + hover motion resolve through `useMotionPreset(variants.tapPress/hover, transitions.micro)`; no spring/scale numbers are inlined in the component.
    - All 7 CVA variants and 8 sizes and the data-slot/variant/size attributes are unchanged; asChild single-child contract preserved.
    - `npm run build` exits 0; no file under design-system/tokens/ changed.
  </acceptance_criteria>
  <done>Button renders SmoothUI press/hover motion across every rank/variant/size, degrades under reduced motion via the presets, and the full app builds green — the foundation is proven end-to-end on the busiest primitive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the two foundation additions (variants.focusPop, getSlideOffset) + tests + MOTION.md</name>
  <files>src/lib/motion.ts, src/lib/motion.test.ts, design-system/MOTION.md</files>
  <read_first>src/lib/motion.ts (full — the `variants` object and the export list), src/lib/motion.test.ts (existing inventory + reduced-motion-contract tests to extend), design-system/MOTION.md (the "Variant pairs" table + Reduced-motion section), .planning/phases/10-component-reshape/10-RESEARCH.md ("Foundation Additions Needed" section — exact shapes), .planning/phases/09-smoothui-foundation/09-REVIEW.md (IN-01/IN-02 notes, address opportunistically only if trivial)</read_first>
  <behavior>
    - `variants.focusPop.full` supplies a `whileFocus` scale-up; `variants.focusPop.reduced.whileFocus` is an empty object (no transform keys) so the existing reduced-motion `it.each` contract test passes for it automatically.
    - `getSlideOffset("top")` → `{ x: 0, y: "-100%" }`; `"bottom"` → `{ x: 0, y: "100%" }`; `"left"` → `{ x: "-100%", y: 0 }`; `"right"` → `{ x: "100%", y: 0 }`.
    - `getSlideOffset` is typed to accept only the four Radix sheet sides ("top"|"right"|"bottom"|"left").
  </behavior>
  <action>Add to `src/lib/motion.ts`, per the RESEARCH "Foundation Additions Needed" shapes: (1) a `focusPop` entry in the `variants` object — `full: { whileFocus: { scale: 1.01 } }`, `reduced: { whileFocus: {} }` — with a one-line comment noting it is Input's focus pop (FORM-01), the only whileFocus pair; (2) an exported pure function `getSlideOffset(side: "top" | "right" | "bottom" | "left")` returning the off-screen `{ x, y }` transform per side (a structural constant helper, same category as `clipCorner`), with a comment noting it feeds Sheet's per-side `initial`/`exit` (SURF-02). Do NOT inline either value at a consumer — this module is the single source (CONTEXT.md "add it to the foundation, do not inline it locally"). Extend `src/lib/motion.test.ts` with: a case asserting `variants.focusPop` exists and its `reduced.whileFocus` has no transform keys; and a case asserting `getSlideOffset` returns the exact `{x,y}` for all four sides. Document both in `design-system/MOTION.md`: add a `focusPop` row to the "Variant pairs" table (Full → Reduced + typical usage "Input focus pop"), and add a short "Helper functions" subsection under Motion describing `getSlideOffset(side)`. While editing motion.ts, if 09-REVIEW.md IN-01 (duplicated magic number) or IN-02 (preset-composition footgun docs) is a trivial one-line fix, apply it; otherwise leave it — do not expand scope.</action>
  <verify>
    <automated>npx vitest run src/lib/motion.test.ts && grep -q 'focusPop' src/lib/motion.ts && grep -q 'getSlideOffset' src/lib/motion.ts && grep -q 'getSlideOffset' design-system/MOTION.md && grep -q 'focusPop' design-system/MOTION.md</automated>
    <fails_when>The motion unit suite fails (focusPop reduced contract or getSlideOffset outputs wrong); OR either new export is absent from motion.ts; OR either addition is undocumented in MOTION.md.</fails_when>
  </verify>
  <acceptance_criteria>
    - `variants.focusPop` and `getSlideOffset(side)` are exported from src/lib/motion.ts and consumed by nobody inline.
    - motion.test.ts covers focusPop's reduced-motion contract and getSlideOffset's four-direction output; `npx vitest run src/lib/motion.test.ts` passes.
    - MOTION.md documents focusPop (Variant pairs table) and getSlideOffset (Helper functions subsection).
    - No colour/type token file changed.
  </acceptance_criteria>
  <done>The Forms wave (Input/focusPop) and Portal-surfaces wave (Sheet/getSlideOffset) can draw their motion from the foundation with zero local magic numbers, and the additions are unit-tested and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user (browser) → client component | Button is a presentational client component; no server, no new input surface, no data handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01-01 | Tampering | button.tsx reshape breaking a consuming page | low | mitigate | `npm run build` gate compiles every Button call site (home/upload/download/DialogFooter/AppShell) — a broken reshape fails the gate before merge. |
| T-10-01-02 | Denial of Service (UX) | reduced-motion not honored, motion sickness | low | mitigate | Motion resolved only via useMotionPreset; unit-tested reduced contract on the foundation module. |

No package installs in this plan — supply-chain (T-*-SC) not applicable.
</threat_model>

<verification>
- `npm run build` exits 0.
- `npx vitest run src/lib/motion.test.ts` passes (foundation additions covered).
- `git status --porcelain -- design-system/tokens/` is empty (no colour/type token changes).
- Full suite regression: `npm test` still reports 123 passing (baseline unchanged; only motion.test.ts gains cases).
</verification>

<success_criteria>
Button renders SmoothUI press/hover motion across all ranks/variants/sizes with the app building green; `variants.focusPop` and `getSlideOffset(side)` exist, are tested, and are documented — unblocking Waves 2 consumers (Input, Sheet).
</success_criteria>

## Artifacts this phase produces (this plan)
- `src/components/ui/button.tsx` — reshaped tracer primitive.
- `src/lib/motion.ts` — `+ variants.focusPop`, `+ getSlideOffset(side)`.
- `src/lib/motion.test.ts` — coverage for both additions.
- `design-system/MOTION.md` — documented focusPop row + Helper functions subsection.

<output>
Create `.planning/phases/10-component-reshape/10-01-SUMMARY.md` when done.
</output>
