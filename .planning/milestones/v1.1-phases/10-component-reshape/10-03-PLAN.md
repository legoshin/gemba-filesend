---
phase: 10-component-reshape
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/ui/progress.tsx
  - src/components/ui/sonner.tsx
autonomous: true
requirements: [FDBK-01, FDBK-02]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The progress bar fills with SmoothUI motion, driven presentation-only off the existing live value — the encryption/upload/download state that produces the value is untouched (FDBK-02)"
    - "Toasts render with SmoothUI toast geometry via sonner's own CSS surface, keeping sonner's built-in reduced-motion behaviour (FDBK-01)"
  artifacts:
    - path: "src/components/ui/progress.tsx"
      provides: "motion-driven progress indicator (transitions.fill) + container reveal (variants.progress)"
      contains: "transitions.fill"
    - path: "src/components/ui/sonner.tsx"
      provides: "SmoothUI-shaped toast via toastOptions.classNames using shape tokens"
      contains: "toastOptions"
  key_links:
    - from: "src/components/ui/progress.tsx"
      to: "src/lib/motion.ts"
      via: "transitions.fill + variants.progress — the purpose-built progress presets"
      pattern: "transitions\\.fill"
    - from: "src/components/ui/sonner.tsx"
      to: "src/lib/shape.ts"
      via: "toastOptions.classNames composes shape.card / shape.ring onto the toast surface"
      pattern: "toastOptions"
---

<objective>
Reshape the two motion-bearing feedback surfaces: the Progress bar (FDBK-02, driven off the live encryption/upload/download value, presentation-only) and the sonner Toaster (FDBK-01, a CSS-surface re-skin — NOT a motion.* wrap, because sonner animates its own DOM). Skeleton/loading treatment (FDBK-03) is handled in 10-07 alongside its download-page loading-state consumer.

Purpose: Give the feedback family SmoothUI geometry/motion without weakening the encryption-adjacent progress value path.
Output: A motion-filled Progress and a shape-reskinned Toaster.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-component-reshape/10-RESEARCH.md
@.planning/phases/10-component-reshape/10-PATTERNS.md
@src/lib/motion.ts
@src/lib/shape.ts
@src/lib/use-motion-preset.ts
@src/components/ui/progress.tsx
@src/components/ui/sonner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reshape the Progress bar with motion fill (FDBK-02)</name>
  <files>src/components/ui/progress.tsx</files>
  <read_first>src/components/ui/progress.tsx (full, 31 lines — current CSS `transition-all` + inline `style` translateX), src/lib/motion.ts (transitions.fill, variants.progress), .planning/phases/10-component-reshape/10-RESEARCH.md (Component Map "Progress" row + Common Pitfall 5: spring-lag on a rapidly-updating live value)</read_first>
  <action>Reshape `src/components/ui/progress.tsx`: keep `ProgressPrimitive.Root` and the `value` prop wiring EXACTLY as-is (the value is fed by the live upload/download encryption-progress state in the pages — presentation-only, do not touch the state or its producers). Replace the `ProgressPrimitive.Indicator`'s raw CSS `transition-all` + inline `style={{ transform: translateX(...) }}` with a motion indicator (hoist `motion.create(ProgressPrimitive.Indicator)` at module scope) that animates its transform toward `value` using `transition={transitions.fill}`. Optionally reveal `ProgressPrimitive.Root` on mount via `useMotionPreset(variants.progress, transitions.snappy)`. Add `"use client"` if the file is not already a client component. CAUTION (Pitfall 5): `transitions.fill` is tuned for a single 0→100 reveal; the live consumer updates the value many times per second. The executor MUST sanity-check the fill does not visibly "chase" a fast-moving value (simulate a fast transfer). If lag is visible, the correct fix is a stiffer NAMED preset added to `src/lib/motion.ts` (do not inline a one-off spring, do not silently disable the animation) — record that in the SUMMARY. No colour/type/radius value changes.</action>
  <verify>
    <automated>grep -q 'transitions.fill' src/components/ui/progress.tsx && grep -q 'motion.create\|motion\.' src/components/ui/progress.tsx && npm run build && test -z "$(git status --porcelain -- design-system/tokens/ src/lib/crypto.ts src/lib/storage.ts src/lib/server-storage.ts src/lib/blob-storage.ts)"</automated>
    <fails_when>Progress indicator does not use transitions.fill via a motion element; OR `npm run build` errors; OR a token or crypto/storage file was modified (progress is presentation-only).</fails_when>
  </verify>
  <acceptance_criteria>
    - progress.tsx indicator animates its fill via a hoisted motion element + `transitions.fill`; the `value` prop and Root wiring are unchanged.
    - No crypto/storage/token file modified.
    - `npm run build` exits 0.
    - SUMMARY records the fast-transfer sanity check result (and any named stiffer preset if one was needed).
  </acceptance_criteria>
  <done>The progress bar fills with SmoothUI motion off the existing live value, the encryption/transfer path is untouched, and rapid-update lag was checked.</done>
</task>

<task type="auto">
  <name>Task 2: Re-skin sonner toasts to SmoothUI geometry (FDBK-01)</name>
  <files>src/components/ui/sonner.tsx</files>
  <read_first>src/components/ui/sonner.tsx (full, 40 lines — Toaster wrapper with style CSS vars), .planning/phases/10-component-reshape/10-RESEARCH.md (Common Pitfall 2: sonner is CSS-driven, verified from its styles.css — do NOT wrap in motion.*; Don't Hand-Roll row on toast reduced-motion), src/lib/shape.ts (shape.card, shape.ring)</read_first>
  <action>Re-skin `src/components/ui/sonner.tsx` at the CSS surface ONLY. Pass `toastOptions={{ classNames: { toast: cn(shape.card, ...) } }}` (and any peer class slots needed) composing the `shape.card`/`shape.ring` tokens onto the toast surface; sonner's injected styles win the cascade, so these Tailwind utilities need the `!` override marker to take effect (per sonner's own styling docs). Do NOT import `motion/react` or wrap `<Sonner>` / its children in `motion.*` — sonner renders its own portal DOM and animates via hand-written CSS transitions; there is no child element to wrap (Pitfall 2). Do NOT add a custom reduced-motion check: sonner ships its own `@media (prefers-reduced-motion)` block that already satisfies FND-03 (Don't Hand-Roll). Keep the existing `theme`/`icons`/CSS-var `style` props intact; keep radius aligned to the existing `--radius-lg` token (== shape.card). No colour/type token changes.</action>
  <verify>
    <automated>grep -q 'toastOptions' src/components/ui/sonner.tsx && test "$(grep -c "from \"motion/react\"" src/components/ui/sonner.tsx)" = "0" && npm run build && test -z "$(git status --porcelain -- design-system/tokens/)"</automated>
    <fails_when>sonner.tsx does not use toastOptions to apply shape, OR it imports motion/react (Pitfall 2 violation), OR `npm run build` errors, OR a token file changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - sonner.tsx applies SmoothUI toast geometry via `toastOptions.classNames` using shape tokens; no motion/react import.
    - No custom reduced-motion handling added (sonner's own media query is relied upon).
    - `npm run build` exits 0; no token file changed.
  </acceptance_criteria>
  <done>Toasts render with SmoothUI geometry through sonner's supported CSS surface, keeping sonner's built-in enter/exit + reduced-motion behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| encryption/upload state → Progress (presentation) | Progress renders an already-computed percentage; it must not read or alter the crypto/transfer state that produces it. |
| user (browser) → Toaster | sonner renders client-side; no server, no new input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03-01 | Tampering | Progress reshape reaching into the encryption/transfer value path | medium | mitigate | Presentation-only edit; git-diff guard asserts crypto/storage files unmodified; `value` prop wiring preserved. |
| T-10-03-02 | DoS (UX) | spring-lag Progress chasing a fast live value | low | mitigate | Pitfall-5 fast-transfer sanity check; stiffer NAMED preset if needed, never disable silently. |

No package installs — supply-chain not applicable.
</threat_model>

<verification>
- `npm run build` exits 0.
- Progress uses transitions.fill; sonner uses toastOptions and imports no motion/react.
- `git status --porcelain` shows no changes under design-system/tokens/ or the crypto/storage libs.
- Regression: `npm test` still 123 passing.
</verification>

<success_criteria>
Progress fills with SmoothUI motion presentation-only (encryption path intact), and toasts carry SmoothUI geometry via sonner's CSS surface with reduced-motion honored by the library.
</success_criteria>

## Artifacts this phase produces (this plan)
- `src/components/ui/progress.tsx`, `src/components/ui/sonner.tsx` — reshaped feedback surfaces.

<output>
Create `.planning/phases/10-component-reshape/10-03-SUMMARY.md` when done.
</output>
