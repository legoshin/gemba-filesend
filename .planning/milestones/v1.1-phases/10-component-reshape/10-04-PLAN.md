---
phase: 10-component-reshape
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/avatar.tsx
  - src/components/ui/separator.tsx
autonomous: true
requirements: [SURF-01, SURF-04]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Card reshapes to SmoothUI geometry (its one literal rounded-lg gap closed to shape.card) with entrance/hover motion (SURF-01)"
    - "Badge matches its already-migrated sibling chip.tsx exactly, preserving all CVA variants (SURF-04)"
    - "Avatar and Separator carry SmoothUI geometry (Avatar optional fade-in; Separator shape-only) (SURF-04)"
    - "Motion degrades under prefers-reduced-motion via the Phase 9 presets"
  artifacts:
    - path: "src/components/ui/card.tsx"
      provides: "Card entrance/hover motion + rounded-lg → shape.card fix (the PATTERNS literal-gap finding)"
      contains: "fadeSlideUp"
    - path: "src/components/ui/badge.tsx"
      provides: "SmoothUI Badge mirroring chip.tsx's motion.create(Slot.Root) + useMotionPreset pattern"
      contains: "useMotionPreset"
    - path: "src/components/ui/avatar.tsx"
      provides: "Avatar SmoothUI geometry (rounded-full == shape.pill) + optional image fade-in"
    - path: "src/components/ui/separator.tsx"
      provides: "Separator shape-token alignment (no motion)"
  key_links:
    - from: "src/components/ui/badge.tsx"
      to: "src/components/chip.tsx"
      via: "identical motion.create(Slot.Root) hoist + useMotionPreset(variants.fadeSlideUp, transitions.snappy)"
      pattern: "useMotionPreset"
    - from: "src/components/ui/card.tsx"
      to: "src/lib/shape.ts"
      via: "shape.card replaces the raw rounded-lg on the Card root"
      pattern: "shape\\.card|radius-lg"
---

<objective>
Reshape the low-risk surface components (no Radix portal, no forceMount): Card (SURF-01) and Badge/Avatar/Separator (SURF-04). These reuse existing presets directly and close the one confirmed literal geometry gap in the codebase — `card.tsx`'s raw `rounded-lg`.

Purpose: Bring the simple surfaces up to the foundation pattern; Badge is chip.tsx's un-migrated twin.
Output: Reshaped Card, Badge, Avatar, Separator.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-component-reshape/10-RESEARCH.md
@.planning/phases/10-component-reshape/10-PATTERNS.md
@src/components/chip.tsx
@src/lib/motion.ts
@src/lib/shape.ts
@src/lib/use-motion-preset.ts
@src/components/ui/card.tsx
@src/components/ui/badge.tsx
@src/components/ui/avatar.tsx
@src/components/ui/separator.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reshape Card + close the rounded-lg gap (SURF-01)</name>
  <files>src/components/ui/card.tsx</files>
  <read_first>src/components/ui/card.tsx (full, ~92 lines — Card + 6 sub-components; the Card root uses raw `rounded-lg`), src/components/chip.tsx (motion wiring), src/lib/motion.ts (variants.fadeSlideUp, variants.hover, transitions.snappy), src/lib/shape.ts (shape.card == rounded-[var(--radius-lg)]), .planning/phases/10-component-reshape/10-PATTERNS.md (Group F — the literal-gap finding)</read_first>
  <action>Reshape `src/components/ui/card.tsx`: add `"use client"` (the file is server-safe today; motion requires the boundary). On the outer `Card` component ONLY, swap the `<div>` for `motion.div` and spread `useMotionPreset(variants.fadeSlideUp, transitions.snappy)` for entrance; where a Card is used as an interactive/clickable surface you may additionally spread `variants.hover` — but keep it a single resolved preset per element, no inline literals. Close the geometry gap: replace the Card root's raw `rounded-lg` with `shape.card` (== `rounded-[var(--radius-lg)]`, 16px) per the "no magic numbers" rule — this is the one confirmed literal radius gap vs. shape.ts. Leave the sub-components (`CardHeader`/`CardTitle`/`CardDescription`/`CardAction`/`CardContent`/`CardFooter`) as plain `<div>`s — no motion on children. Keep all `data-slot` attributes and every other className unchanged. No colour/type token changes.</action>
  <verify>
    <automated>grep -q '"use client"' src/components/ui/card.tsx && grep -q 'fadeSlideUp' src/components/ui/card.tsx && grep -Eq 'shape\.card|rounded-\[var\(--radius-lg\)\]' src/components/ui/card.tsx && npm run build && test -z "$(git status --porcelain -- design-system/tokens/)"</automated>
    <fails_when>Card lacks the client directive / fadeSlideUp entrance, OR the root still uses the raw rounded-lg instead of shape.card/radius-lg, OR `npm run build` errors (Card renders on home/upload/download), OR a token file changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - Card root is a motion.div with `useMotionPreset(variants.fadeSlideUp, transitions.snappy)` entrance.
    - The root radius is `shape.card` (or its `--radius-lg` var), not the raw `rounded-lg`.
    - Sub-components remain plain divs; data-slot attributes intact.
    - `npm run build` exits 0; no token file changed.
  </acceptance_criteria>
  <done>Card enters with SmoothUI motion, the literal radius gap is closed to the shape token, and every page rendering Card builds green.</done>
</task>

<task type="auto">
  <name>Task 2: Reshape Badge to mirror chip.tsx (SURF-04)</name>
  <files>src/components/ui/badge.tsx</files>
  <read_first>src/components/ui/badge.tsx (full — CVA + `Comp = asChild ? Slot.Root : "span"`), src/components/chip.tsx (the exact transform to apply — Badge is chip's un-migrated sibling), src/lib/motion.ts (variants.fadeSlideUp, transitions.snappy)</read_first>
  <action>Reshape `src/components/ui/badge.tsx` by applying the IDENTICAL transform `chip.tsx` already received (PATTERNS Group A): add `"use client"`; hoist `const MotionSlot = motion.create(Slot.Root)` at module scope with chip.tsx's explanatory comment; set `const Comp = asChild ? MotionSlot : motion.span`; resolve `const motionProps = useMotionPreset(variants.fadeSlideUp, transitions.snappy)` and spread `{...motionProps}` before `{...props}`. Add the same `NativeSpanProps` Omit of Motion-colliding native handlers chip.tsx uses. Keep the ENTIRE `badgeVariants` CVA block, all `data-slot`/`data-variant` attributes, and the `asChild` single-child contract byte-for-byte — the variant/size names are locked public API. No colour/type/radius value changes (badge already uses shape-aligned radii).</action>
  <verify>
    <automated>grep -q '"use client"' src/components/ui/badge.tsx && grep -q 'motion.create' src/components/ui/badge.tsx && grep -q 'useMotionPreset' src/components/ui/badge.tsx && npm run build && test -z "$(git status --porcelain -- design-system/tokens/)"</automated>
    <fails_when>Badge lacks the client directive / hoisted motion.create / useMotionPreset wiring, OR `npm run build` errors (Badge is consumed by recipient-chip-input), OR a token file changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - badge.tsx mirrors chip.tsx: client directive, hoisted `motion.create(Slot.Root)`, `useMotionPreset(variants.fadeSlideUp, transitions.snappy)`, NativeSpanProps Omit.
    - All CVA variants + data-slot/variant attributes + asChild contract preserved.
    - `npm run build` exits 0; no token file changed.
  </acceptance_criteria>
  <done>Badge is visually + behaviourally consistent with its migrated sibling chip.tsx.</done>
</task>

<task type="auto">
  <name>Task 3: Reshape Avatar + Separator geometry (SURF-04)</name>
  <files>src/components/ui/avatar.tsx, src/components/ui/separator.tsx</files>
  <read_first>src/components/ui/avatar.tsx (full, ~109 lines — Avatar/Image/Fallback/Badge/Group; all rounded-full == shape.pill; zero live consumers today), src/components/ui/separator.tsx (full, ~28 lines — shape-only leaf), src/lib/motion.ts (variants.fadeSlideUp), .planning/phases/10-component-reshape/10-PATTERNS.md (Group E)</read_first>
  <action>Avatar (`src/components/ui/avatar.tsx`): its `rounded-full` already equals `shape.pill` — confirm alignment and make no radius value change. Add a LIGHT optional fade-in on the loaded image: gate a `motion` wrapper via `AvatarPrimitive.Image`'s `onLoadingStatusChange` and spread `useMotionPreset(variants.fadeSlideUp, transitions.snappy)` (add `"use client"` if not already present). Keep this minimal — Avatar has zero live consumers, so it is scaffold-only; do not over-build. Separator (`src/components/ui/separator.tsx`): shape-only, no motion — confirm it carries no divergent hardcoded radius and leave it byte-for-byte if already token-consistent. No colour/type token changes on either.</action>
  <verify>
    <automated>npm run build && test -z "$(git status --porcelain -- design-system/tokens/)"</automated>
    <fails_when>`npm run build` errors (Avatar/Separator are type-checked project-wide by next build even with zero consumers), OR a token file changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - Avatar retains rounded-full geometry and gains a minimal image fade-in via useMotionPreset (or is confirmed shape-consistent with a documented no-op if fade-in is skipped).
    - Separator carries no divergent hardcoded radius.
    - `npm run build` exits 0; no token file changed.
  </acceptance_criteria>
  <done>Avatar and Separator carry SmoothUI geometry; Avatar optionally fades its image in, and both compile project-wide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user (browser) → client surface | All four are presentational client/leaf components; no server, no input handling, no data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04-01 | Tampering (UX regression) | Card/Badge reshape breaking a consuming page | low | mitigate | `npm run build` compiles every Card (home/upload/download) and Badge (recipient-chip-input) call site. |
| T-10-04-02 | DoS (UX) | reduced-motion not honored | low | mitigate | Motion resolved only via useMotionPreset. |

No package installs — supply-chain not applicable.
</threat_model>

<verification>
- `npm run build` exits 0.
- Card entrance + shape.card fix present; Badge mirrors chip wiring.
- `git status --porcelain -- design-system/tokens/` empty.
- Regression: `npm test` still 123 passing.
</verification>

<success_criteria>
Card, Badge, Avatar, Separator render SmoothUI geometry (Card gains entrance/hover motion and its rounded-lg gap is closed; Badge matches chip); all build green and honor reduced motion.
</success_criteria>

## Artifacts this phase produces (this plan)
- `src/components/ui/card.tsx`, `badge.tsx`, `avatar.tsx`, `separator.tsx` — reshaped low-portal surfaces.

<output>
Create `.planning/phases/10-component-reshape/10-04-SUMMARY.md` when done.
</output>
