---
phase: 11-page-motion
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/app/upload/page.tsx
autonomous: true
requirements: [MOT-01, MOT-02]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "The upload page animates its primary content (Select Files / Options / action cards, and the Upload-Complete card) in on mount with the shared PageEntrance stagger."
    - "A thin scroll-progress bar tracks scroll position on the upload page (which is long)."
    - "Entrance + scroll-progress degrade correctly under prefers-reduced-motion (inherited from the shared components + app-root MotionConfig)."
    - "No crypto/storage/multi-file logic, upload flow, live progress UI, or colour/type token is modified."
  artifacts:
    - src/app/upload/page.tsx
  key_links:
    - "ScrollProgress is mounted ONCE in the upload page's primary (long) content return as a fixed overlay; it does not wrap or alter the upload/encryption logic."
    - "PageEntrance/PageEntranceItem wrap the presentation section cards only — the FileDropzone, Dialog, and blob-upload calls keep their exact behavior."
---

<objective>
Apply the Wave-1 tracer components to the upload page: wrap its primary content in the shared `PageEntrance` stagger (MOT-01, upload) and mount the shared `ScrollProgress` bar (MOT-02, upload — the upload page is long and scrolls).

Purpose: Deliver the proven entrance + scroll-progress pattern on the first long page. This is a surgical, presentation-only wrapping — the client-side-encryption path, the Vercel Blob upload flow, and the live upload-progress UI are untouched.
Output: `src/app/upload/page.tsx` renders staggered entrance on its cards and a scroll-progress bar, with `npm run build` + `npm test` green.

Consumes (from 11-01): `PageEntrance`/`PageEntranceItem` from "@/components/page-entrance"; `ScrollProgress` from "@/components/scroll-progress".
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-page-motion/11-CONTEXT.md
@.planning/phases/11-page-motion/11-01-SUMMARY.md
@src/components/page-entrance.tsx
@src/components/scroll-progress.tsx
@src/app/upload/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap upload primary content in PageEntrance + mount ScrollProgress (surgical)</name>
  <files>src/app/upload/page.tsx</files>
  <read_first>
    - src/app/upload/page.tsx — the `UploadPage` component (from line ~193): the main content `return` (root `<div className="mx-auto max-w-2xl px-4 py-12 sm:px-6 sm:py-16">`, ~line 540) and the Upload-Complete `return` (the success `<Card>` around line 552). Identify the top-level presentation cards (Select Files, Options, action card) that should stagger in.
    - src/components/page-entrance.tsx — `PageEntrance({children,className?})` (container) and `PageEntranceItem({children,className?})` (item; inherits parent labels).
    - src/components/scroll-progress.tsx — `ScrollProgress()` (no props, fixed overlay).
    - 11-01-SUMMARY.md — exact component signatures + staggerContainer value.
  </read_first>
  <action>
    In `UploadPage`'s primary content return, replace the root `<div className="mx-auto max-w-2xl px-4 py-12 sm:px-6 sm:py-16">` with `<PageEntrance className="mx-auto max-w-2xl px-4 py-12 sm:px-6 sm:py-16">`, and wrap each top-level presentation card (the Select Files card, the Options card, and the action/submit card) in a `<PageEntranceItem>`. Do the same for the Upload-Complete success return (wrap its root container in `<PageEntrance>` and the success `<Card>` in a `<PageEntranceItem>`) so both states animate in. Preserve every existing className, prop, handler, and child byte-for-byte — you are only inserting wrapper elements.

    Mount `<ScrollProgress />` ONCE as the first child inside the primary content return's top-level container (it is `fixed`, so DOM position does not affect layout). Do not mount a second copy; the Upload-Complete state is short and does not need it, but a single mount in the main long state is sufficient. Import `ScrollProgress` from "@/components/scroll-progress" and `PageEntrance`, `PageEntranceItem` from "@/components/page-entrance".

    Do NOT modify: the FileDropzone, the Dialog, the `upload(...)` (@vercel/blob/client) call, any encryption/crypto/storage/multi-file logic, the live `<Progress>` upload-progress UI behavior, or any colour/type token. This is a presentation-wrapper change only. The page is already `"use client"`, so no boundary change is needed. Do not use `motion.create(...)` here — the wrapping is entirely via the shared components.
  </action>
  <verify>
    <automated>grep -q 'PageEntrance' src/app/upload/page.tsx && grep -q 'PageEntranceItem' src/app/upload/page.tsx && grep -q 'ScrollProgress' src/app/upload/page.tsx && grep -q 'from "@/components/page-entrance"' src/app/upload/page.tsx && grep -q 'from "@/components/scroll-progress"' src/app/upload/page.tsx && npm run build && npm test && test -z "$(git status --porcelain -- src/lib/crypto.ts src/lib/storage.ts src/lib/server-storage.ts src/lib/blob-storage.ts src/lib/multi-file.ts design-system/tokens/)"</automated>
    <fails_when>upload/page.tsx does not import or use PageEntrance/PageEntranceItem/ScrollProgress; `npm run build` fails; `npm test` regresses below the green baseline; or ANY protected file (crypto.ts, storage.ts, server-storage.ts, blob-storage.ts, multi-file.ts, design-system/tokens/) shows a git diff.</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/app/upload/page.tsx` wraps its primary content cards (and the Upload-Complete card) in `PageEntrance` + `PageEntranceItem` from the shared component.
    - `<ScrollProgress />` is mounted exactly once in the primary content return.
    - All upload/crypto/blob/dialog/live-progress logic and every className/prop is byte-for-byte unchanged apart from the inserted wrapper elements.
    - `npm run build` and `npm test` are green; no diff to any protected crypto/storage/multi-file/token path.
  </acceptance_criteria>
  <done>The upload page staggers its primary content in on load and shows a scroll-progress bar as the user scrolls; reduced motion is honored; the encryption boundary, upload flow, and tokens are untouched.</done>
  <reversibility rating="reversible">Inserts shared wrapper elements + one component mount into one file — revert by unwrapping.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ upload page render | Entrance/scroll motion is client-side presentation; reads no untrusted input. |
| motion wrappers ↔ upload/encryption flow | Wrappers must not reach into the @vercel/blob upload call or the client-side-encryption path (key never leaves the client). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Tampering | src/lib/crypto.ts, storage.ts, multi-file.ts (via upload/page.tsx) | high | mitigate | `<verify>` git-diff-guard asserts zero diff to crypto/storage/multi-file; the change only inserts presentation wrappers around existing cards. |
| T-11-05 | Tampering | design-system/tokens/ (colour/type) | medium | mitigate | git-diff-guard asserts zero token diff; scroll bar/entrance reuse existing tokens/presets. |
| T-11-06 | Denial of Service | upload page scroll handler (ScrollProgress) | low | accept | Passive MotionValue-driven bar, no per-scroll state churn; reduced-motion drops the spring. |
</threat_model>

<verification>
- `npm run build` green.
- `npm test` full suite green, no regression (baseline ≥127).
- `test "$(npx tsc --noEmit 2>&1 | grep 'error TS' | grep -vc 'crypto.test.ts')" = "0"`.
- git-diff-guard: no diff to crypto/storage/server-storage/blob-storage/multi-file or design-system/tokens.
- Manual (Phase 12, not a gate): upload cards stagger in; scroll bar tracks; live upload progress and encryption unchanged in light/dark/system.
</verification>

<success_criteria>
- Upload page animates its primary content in on load (MOT-01, upload).
- A scroll-progress bar tracks scroll on the upload page (MOT-02, upload).
- Upload flow, live progress UI, encryption boundary, and colour/type tokens are unchanged.
</success_criteria>

<output>
Create `.planning/phases/11-page-motion/11-02-SUMMARY.md` when done.
</output>
